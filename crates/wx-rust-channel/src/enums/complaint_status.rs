//! 视频号小店 枚举（对应 Java `ComplaintStatus`）。

/// ComplaintStatus（对应 Java `me.chanjar.weixin.channel.enums.ComplaintStatus`，Java 侧无任何常量）。
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum ComplaintStatus {}
