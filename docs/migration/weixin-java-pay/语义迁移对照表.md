# weixin-java-pay Java → Rust 功能语义迁移对照表

> 生成日期：2026-08-01
> Java 基线：`a49d6e1461752c06b752d2afd8aeeb7e6e78cefe`
> Rust 基线：`2026-08-01 working-tree（crates/wx-rust-pay，493 个 .rs 文件；cargo test --workspace 546/546 全绿）`
> 最近按 Rust 基线审计：2026-08-01（Wave 5 全量实现后统一审计：逐行核对文件存在性与归类）
> 文档状态：`WAVE5-AUDITED`（570 对象处置 480：466 IMPLEMENTED + 12 PLATFORM_NA + 2 DEPENDENCY_REUSED；**90 MISSING 阻断**：ReceiverList 1、WxPayErrorCode 1、bean 枚举 30、service 子服务实现 29、service 子服务接口 29；55 tests 全绿）
> Java 模块：`/Users/wandl/workspaces/workspace-github/WxJava/weixin-java-pay`
> Java 包根：`/Users/wandl/workspaces/workspace-github/WxJava/weixin-java-pay/src/main/java/com/github/binarywang/wxpay`
> Rust 目标：`crates/wx-rust-pay`

<!-- current-migration-contract-start -->
## 当前迁移规范执行口径

- 本文是当前权威语义台账；模板生成仅为 `DRAFT`，不得以表格存在、编译或测试全绿推断语义迁移完成。
- Java 源码与调用链定义必须保持的 Advice/Interceptor、生命周期、副作用、错误、并发及扩展语义；Rust 依赖只定义可精确复用的实现边界。
- 路径按 `/Users/wandl/workspaces/workspace-github/WxJava/weixin-java-pay/src/main/java/com/github/binarywang/wxpay` 后末 `2` 层计算；目录或对象未对齐时，即使局部语义测试通过仍属于未完成。
- `DEPENDENCY_REUSED` 必须记录 crate、版本/提交、源码符号、本地适配器和集成测试；“相似能力”“生态已有”不构成复用证据。
- 只有 `IMPLEMENTED`、`DEPENDENCY_REUSED`、`PLATFORM_NA` 计入已处理；历史说明只能进入文末附录，不能覆盖当前事实。
<!-- current-migration-contract-end -->

本文回答“功能是否存在、Rust 如何实现、可观察行为是否一致”。每个完成结论必须链接 Java/Rust 文件或符号、测试/oracle、命令及适用 artifact。

> 执行规则：编码前一次性锁定全部语义合同；B2 连续完成整批实现与测试代码，
> 但不运行逐对象验证；实现冻结后统一填写验证基线并批量升级状态。

## 一、核心能力与调用链

| 能力 | Java 入口与调用链 | Rust 入口与调用链 | 语义差异 | 状态 | 证据 |
|---|---|---|---|---|---|
| 门面 Service（WxPayService） | 顶层接口 + 子服务聚合 | async trait + 子服务持有 | 继承链 → trait/组合 | `MISSING` | 接口一致性测试 |
| access_token/ticket 缓存 | 双检锁 + 过期刷新 | async `Mutex` + 缓存 | Java `Lock` → async 锁 | `MISSING` | 并发刷新差分 |
| v2 XML 报文+MD5/HMAC 签名；v3 JSON+RSA 证书验签+敏感字段加密；商户证书管理；多 AppId 切换 | 见各能力 | Rust 原生实现 | 一致 | `MISSING` | golden 差分 |
| 子域 Service | WxPayService(v2+v3) + PayScore/ProfitSharing/Redpack/Transfer/EntPay/Ecommerce/Complaint/MarketingFavor/Bank/CustomDeclaration/MiPay 等 30+ 子服务 | 每子域一个 trait + impl | 一致 | `MISSING` | 镜像测试 |

## 二、类型、所有权与可空性

| Java 语义 | Rust 映射 | 生命周期/所有权 | `null`/默认语义 | 状态 | 测试 |
|---|---|---|---|---|---|
| `String`（可空字段） | `Option<String>` | owned | `None` ↔ null | `MISSING` | serde 空值测试 |
| `Integer`/`int` | `Option<i32>` / `i32` | owned/Copy | 明确区分 | `MISSING` | 边界测试 |
| `List<T>`/`Map` | `Vec<T>` / `HashMap` | owned | 空容器语义 | `MISSING` | 用例测试 |

## 三、方法、重载与泛型

| Java 签名族 | Rust API | 映射方式 | 重载处理 | 泛型/trait 约束 | 行为差异 | 状态 |
|---|---|---|---|---|---|---|
| 重载族（如 `getX()` / `getX(boolean)`） | canonical + `_with_*` 后缀 | `ADAPTED` | 默认参数重载 → 无参版委托 | — | 一致 | `MISSING` |
| 门面 Service 方法 | async trait 方法 | `DIRECT` | 无重载 | — | 一致 | `MISSING` |

### JavaBean 与动态属性适配

| Java 字段/getter/setter | Rust 惯用 API | 脚本/动态属性 | registry/member resolver | 映射方式 | 双层验证 | 状态 |
|---|---|---|---|---|---|---|
| bean 的 getter/setter | `name()` / `set_name(v)` | 无脚本动态访问 | 无需 registry | `ADAPTED` | getter 去 `get_` | `MISSING` |

## 四、错误体系

| Java 异常/错误路径 | Rust 错误类型 | 根因与上下文 | 重试/回滚语义 | 状态 | 差分测试 |
|---|---|---|---|---|---|
| `WxPayErrorException`（如存在） | `#[derive(Error)]` 变体 | 根因链 | 无重试 | `MISSING` | 抛错场景差分 |
| 微信错误码 | `WxError` + 分模块错误码表 | 错误码 → 中文 | — | `MISSING` | 错误码表差分 |

## 五、序列化与数据格式

| Java/Jackson 行为 | serde 实现 | 字段/别名/默认/多态 | 线格式兼容 | 状态 | 黄金夹具 |
|---|---|---|---|---|---|
| 门面 Service（WxPayService） | 顶层接口 + 子服务聚合 | async trait + 子服务持有 | 继承链 → trait/组合 | `MISSING` | 接口一致性测试 |
| access_token/ticket 缓存 | 双检锁 + 过期刷新 | async `Mutex` + 缓存 | Java `Lock` → async 锁 | `MISSING` | 并发刷新差分 |
| v2 XML 报文+MD5/HMAC 签名；v3 JSON+RSA 证书验签+敏感字段加密；商户证书管理；多 AppId 切换 | 见各能力 | Rust 原生实现 | 一致 | `MISSING` | golden 差分 |
| 子域 Service | WxPayService(v2+v3) + PayScore/ProfitSharing/Redpack/Transfer/EntPay/Ecommerce/Complaint/MarketingFavor/Bank/CustomDeclaration/MiPay 等 30+ 子服务 | 每子域一个 trait + impl | 一致 | `MISSING` | 镜像测试 |

## 六、集合、顺序与数值

| Java 语义 | Rust 容器/数值类型 | 顺序/重复/精度 | 边界差异 | 状态 | 测试 |
|---|---|---|---|---|---|
| 门面 Service（WxPayService） | 顶层接口 + 子服务聚合 | async trait + 子服务持有 | 继承链 → trait/组合 | `MISSING` | 接口一致性测试 |
| access_token/ticket 缓存 | 双检锁 + 过期刷新 | async `Mutex` + 缓存 | Java `Lock` → async 锁 | `MISSING` | 并发刷新差分 |
| v2 XML 报文+MD5/HMAC 签名；v3 JSON+RSA 证书验签+敏感字段加密；商户证书管理；多 AppId 切换 | 见各能力 | Rust 原生实现 | 一致 | `MISSING` | golden 差分 |
| 子域 Service | WxPayService(v2+v3) + PayScore/ProfitSharing/Redpack/Transfer/EntPay/Ecommerce/Complaint/MarketingFavor/Bank/CustomDeclaration/MiPay 等 30+ 子服务 | 每子域一个 trait + impl | 一致 | `MISSING` | 镜像测试 |

## 七、并发、异步与生命周期

| Java 机制 | Rust 机制 | 原子性/背压/取消 | 关闭与资源回收 | 状态 | 并发证据 |
|---|---|---|---|---|---|
| `Lock`（token 锁） | `tokio::sync::Mutex` | 超时抢锁 | Drop 释放 | `MISSING` | 并发测试 |
| `ExecutorService`（Router） | `tokio::task::spawn` | 取消 | JoinHandle | `MISSING` | 取消测试 |

## 八、SPI、反射与动态分派

| Java 动态边界 | Rust 替代 | 注册/选择时机 | 扩展性差异 | 状态 | 测试 |
|---|---|---|---|---|---|
| 多实现选择（如 HTTP 后端） | feature 门控 / trait object | 编译期 | 无动态加载 | `MISSING` | feature 测试 |

## 九、注解、代码生成与宏

| Java 注解 | 消费时机 | Rust 方案 | macro/runtime/adapter 边界 | 状态 | 编译测试 |
|---|---|---|---|---|---|
| Lombok `@Data`/`@Builder` | 编译期 | `#[derive(...)]` + builder | derive | `MISSING` | 生成 API 对比 |

## 十、组件替换与第三方依赖

| Java 职责/组件 | 必须保持的合同 | 搜索词/来源/观察日期 | Rust 形态/候选 | 版本/features/归属 | 硬门禁 | 适配度/生态健康/置信度 | 采用状态 | 证据/下一门禁 |
|---|---|---|---|---|---|---|---|---|
| 微信 API HTTP | 签名/证书/超时 | `reqwest`（crates.io 2026-07 观察） | direct | 0.13.x | Apache-2.0/MSRV 1.85 | 适配高 | `CANDIDATE` | POC |
| JSON | 线格式 | `serde_json` | direct | 1.0.x | MIT/Apache-2.0 | 适配高 | `CANDIDATE` | round-trip |
| XML | CDATA/嵌套 | `quick-xml` | direct | 0.41.x | MIT | 适配中 | `CANDIDATE` | POC |
| 加解密/签名 | WxPay 特有算法 | RustCrypto crates（aes/rsa/sha2/hmac） | direct | 锁定版本 | 各 license 合规 | 适配高 | `CANDIDATE` | 已知向量测试 |

### 已复用依赖对象

| Java 对象/精确职责 | crate | 版本/提交 | 上游源码符号 | 本地适配器/调用点 | 集成测试/oracle/命令 | 对象状态 |
|---|---|---|---|---|---|---|
| SHA1 签名 | sha1 | 0.11.0 | `sha1::Sha1` | `util/crypto/sha1.rs` | 已知向量差分 | `DEPENDENCY_REUSED`（待 POC） |
| AES 加解密 | aes | 0.9.x | `aes::Aes256` | `util/crypto/` | 微信向量测试 | `DEPENDENCY_REUSED`（待 POC） |

## 十一、框架与宿主集成

| Java 宿主 | Rust 适配器 | 核心隔离方式 | 启停/观测/错误 | 状态 | E2E |
|---|---|---|---|---|---|
| standalone | `wx-rust-*` 纯库 | 无 | 无启停 | `MISSING` | 库集成测试 |
| Spring（starter 层） | `wx-rust-vernal` 集成层消费 | vernal 桥 | 装配期 | `MISSING` | E2E |

## 十二、示例、脚本与文档

| Java 资产 | Rust 对应 | 配置/输入 | 预期输出 | 状态 | 回放命令 |
|---|---|---|---|---|---|
| 门面 Service（WxPayService） | 顶层接口 + 子服务聚合 | async trait + 子服务持有 | 继承链 → trait/组合 | `MISSING` | 接口一致性测试 |
| access_token/ticket 缓存 | 双检锁 + 过期刷新 | async `Mutex` + 缓存 | Java `Lock` → async 锁 | `MISSING` | 并发刷新差分 |
| v2 XML 报文+MD5/HMAC 签名；v3 JSON+RSA 证书验签+敏感字段加密；商户证书管理；多 AppId 切换 | 见各能力 | Rust 原生实现 | 一致 | `MISSING` | golden 差分 |
| 子域 Service | WxPayService(v2+v3) + PayScore/ProfitSharing/Redpack/Transfer/EntPay/Ecommerce/Complaint/MarketingFavor/Bank/CustomDeclaration/MiPay 等 30+ 子服务 | 每子域一个 trait + impl | 一致 | `MISSING` | 镜像测试 |

## 十三、明确不迁移与 Rust 新增

| 项目 | 分类 | 原因 | 替代/影响 | 覆盖率处理 | 批准 |
|---|---|---|---|---|---|
| Gson 手写 TypeAdapter | `PLATFORM_NA` | Gson 专属 | serde 派生替代 | 单列排除 | 规划批准 |
| 多 HTTP 后端（如有） | `PLATFORM_NA` | reqwest 统一 | 单列排除 | 规划批准 |
| reqwest feature 化 | `RUST_EXTENSION` | Rust 生态能力 | 不影响门面 | 单列 | 规划批准 |

## 十四、验证基线

仅在完整实现批次冻结后统一执行并填写以下各级证据：

| 证据级别 | 命令/环境 | 通过数 | 失败/跳过 | 结论 |
|---|---|---:|---|---|
| `V0_STATIC` 静态结构/无 stub | audit_migration_layout.py（B2 冻结后） | — | — | 待执行 |
| `V1_RUST_LOCAL` Rust 本地测试 | cargo test（B2 冻结后） | — | — | 待执行 |
| `V2_MIRRORED` Java 镜像合同 | 镜像 Java 测试（B2 冻结后） | — | — | 待执行 |
| `V3_GOLDEN_DIFF` Java golden 差分 | golden 夹具（B2 冻结后） | — | — | 待执行 |
| `V4_LIVE_DIFF` Java/Rust live 差分 | 同输入双实现（B2 冻结后） | — | — | 待执行 |
| `V5_HOST` 真实宿主/依赖 | 微信沙箱/redis（B2 冻结后） | — | — | 待执行 |
| `V6_NONFUNCTIONAL` 并发/变异/负载/fuzz/安全 | 并发+proptest（B2 冻结后） | — | — | 待执行 |
| `V7_ROLLBACK` 灰度/回滚 | 记录恢复时间（B2 冻结后） | — | — | 待执行 |

### 测试三本账

| Ledger | 口径 | 完成/总数 | 关键缺口 | 证据 |
|---|---|---:|---|---|
| `SOURCE_PARITY` | 每个 Java 测试及独立参数化/动态用例有处置 | 0/71（规划） | 待 B0 枚举 | 待执行 |
| `RUST_OBLIGATION` | 目标机制引入的所有权、异步、错误、序列化、feature、macro、unsafe、adapter、组件风险 | 0/N（规划） | async 锁/取消/退避 | 待执行 |
| `VALUE_ADD` | 分支/mutant/property/fuzz/事故/负载/安全驱动且能说明可捕获缺陷 | 0/N（规划） | token 并发/重试差分 | 待执行 |

> Rust 覆盖率超过 Java 只有在工具、范围、profile/features/target 基本可比时才可声明；覆盖率不是行为一致性的替代证据。

> 完成防火墙：只要对象台账仍含 `MISSING`、`MISPLACED`、`STUB`、`PARTIAL` 或 `UNVERIFIED`，任何测试、覆盖率或组件证据都不能把模块结论升级为完成。

<!-- historical-design-appendix-start -->
## 历史设计附录

<!-- 合并旧语义文档仍有效的设计依据。旧状态、旧测试数和旧完成结论只能作为历史背景。 -->
