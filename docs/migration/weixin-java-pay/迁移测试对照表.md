# weixin-java-pay Java → Rust 迁移测试对照表

> Java 基线：`a49d6e1461752c06b752d2afd8aeeb7e6e78cefe`
> Rust 基线：`2026-08-01 working-tree（crates/wx-rust-pay，493 个 .rs 文件；cargo test --workspace 546/546 全绿）`
> 最近审计：`2026-08-01`
> Java 测试命令：`JAVA_HOME=<corretto-8> mvn -pl weixin-java-pay test`（本会话**未执行**；Java 测试为 TestNG，Maven 默认 surefire 不执行）
> Rust 测试命令：`cargo test --workspace --all-features`（**546/546 通过**，其中 wx-rust-pay 55 个：集成测试 4 文件 53 个 + `bean/xml.rs` 单元测试 2 个）
> Rust 覆盖命令：`cargo llvm-cov --package wx-rust-pay --summary-only`（pay line **40.69%** / fn **24.51%** / region **38.85%**，4674 行；workspace TOTAL line **42.80%** / fn **28.55%** / region **40.67%**，58699 行）

## 零、对象状态完成防火墙

> 权威对象台账：`docs/migration/weixin-java-pay/对象级对照表.md`（570 对象处置 480：466 IMPLEMENTED + 12 PLATFORM_NA + 2 DEPENDENCY_REUSED）
> ⚠️ **90 个对象 `MISSING`（阻断全量交付）**：service 子服务接口 29（26 个 api/sub_services.rs 空 trait
> 占位 + 3 个无对应）、service.impl 子服务实现 29、bean 子包枚举 30、WxPayErrorCode 1、ReceiverList 1。
> 本表 SOURCE_PARITY 中对应子服务测试对象（25 个）按 `NOT_MIRRORED` 如实处置，不因实现面缺失而掩盖。

| `MISSING` | `MISPLACED` | `STUB` | `PARTIAL` | `UNVERIFIED` | 是否阻断迁移完成 |
|---:|---:|---:|---:|---:|---|
| 90 | 0 | 0 | 0 | 0 | **是**——90 对象未实现，模块结论保持未完成（Bean+门面+v3 utils 面已交付） |

## 一、范围与覆盖率口径

| 项目 | Java | Rust | 是否可比 | 说明 |
|---|---|---|---|---|
| 纳入模块/生产文件 | weixin-java-pay 570 个主文件 | wx-rust-pay 493 个 .rs 文件（466 对象 + mod.rs/helper） | 否 | 台账 570 处置 480；90 MISSING |
| 测试对象 | 71 个测试文件（TestNG） | 55 个测试（53 集成 + 2 单元） | 否 | 镜像与增值分层见二/三/四节 |
| 工具与指标 | TestNG（未运行） | cargo-llvm-cov line/fn/region | 否 | — |
| profile/features/target | — | `--workspace --all-features` | — | — |
| 覆盖率（pay） | 未运行 | line **40.69%** / fn **24.51%** / region **38.85%**（4674 行） | 否 | 明细见 `覆盖审计报告.md` |
| 覆盖率（workspace） | 未运行 | line **42.80%** / fn **28.55%** / region **40.67%**（58699 行） | 否 | 同会话全 workspace 汇总 |

## 二、`SOURCE_PARITY`：源测试处置

> 71 个 Java 测试对象全部处置：**20 个镜像/适配** + 43 个未镜像 + 8 个不适用。
> 未镜像 43 项中 25 项对应子服务未实现（台账 MISSING）、3 项 config/基础设施语义已实现但无测试镜像、
> 11 项 bean 线格式测试无 Rust 镜像、3 项功能语义已实现（多商户切换/转账回单/全球单）无镜像、1 项公钥模式未实现。
> Java 测试为 TestNG，本会话未执行（Maven surefire 不跑 TestNG）。

| ID | Java 测试/用例 | 源合同与生产调用链 | Rust 测试 | 处置 | 证据 | 差异/阻塞 |
|---|---|---|---|---|---|---|
| P01 | `bean/result` 线格式组（6 文件：BaseWxPayResult / WxPayOrderQueryResult / WxPayBillResult / WxPayRefundQueryResult / WxPayRefundResult / WxPayUnifiedOrderV3Result） | v2 XML 响应解析/验签/账单解析、v3 下单结果线格式 | `wx_pay_service_impl_test`：`v2_query_order_xml_and_parse`/`v2_download_bill_parse_java_format`/`v2_refund_with_cert_channel_and_sign`/`v3_create_order_jsapi_authorization_and_pay_info` + `bean/xml.rs` 单元（root_children_map 空元素/CDATA） | `MIRRORED` | `V1_RUST_LOCAL` | ① 响应 XML 按 Java dom4j/XPath `toMap()` 语义解析（首个同名元素/CDATA）② 账单按 Java 原始格式逐行解析 ③ v3 结果 JSON 键序对齐 Gson |
| P02 | `bean/notify` 线格式组（6 文件：WxPayNotifyResponse / WxPayOrderNotifyResult / WxPayOrderNotifyUnknownField / WxPayRefundNotifyResult / WxPayRefundNotifyV3Result / WxScanPayNotifyResult） | 通知 XML/JSON 解析、退款 req_info 解密、未知字段容忍 | `v2_order_notify_parse_and_sign_verify`/`v2_refund_notify_ecb_decrypt_java_golden`/`v3_refund_notify_url_default_and_response`/`v2_scan_pay_notify_parse` + `bean/xml.rs` 单元 | `MIRRORED` | `V1_RUST_LOCAL` | ① v2 退款通知 req_info AES-256-ECB 解密以 Java golden 断言（密钥 md5(mchKey) 小写）② 未知字段容忍由 XML map 解析天然覆盖（见 PV03 对应） |
| P03 | `bean/request` 线格式组（4 文件：CombineCloseRequest / WxPayPartnerRefundV3Request / WxPayRefundRequest / WxPayRefundV3Request） | v2/v3 请求 bean 线格式与签名装配 | `combine_close_request_json_golden`/`v2_refund_with_cert_channel_and_sign`/`v3_refund_query_and_partner_query` | `MIRRORED`/`ADAPTED` | `V1_RUST_LOCAL` | partner 退款 v3 请求线格式经 `v3_refund_query_and_partner_query` 适配镜像（PartnerRefundV3 为 `ADAPTED`） |
| P04 | `service/impl/BaseWxPayServiceImplTest`（1 文件） | 门面执行引擎：v2/v3 通道、通知解析、账单下载、签名 | `wx_pay_service_impl_test.rs`（17 个测试，MockServer） | `MIRRORED` | `V1_RUST_LOCAL` | ① Java 用真实商户号 + Guice；Rust 用 MockServer 验证同一契约 ② v2 XML body 签名逐字节黄金断言 ③ v3 Authorization 头格式黄金 |
| P05 | `util/SignUtilsTest`（1 文件） | v2 签名（MD5/HMAC-SHA256 排序拼参） | `v2_sign_utils_golden_official_doc`/`v2_query_comment_hmac_sign` | `MIRRORED` | `V1_RUST_LOCAL` | 官方文档向量黄金重算 |
| P06 | `v3/auth/AutoUpdateCertificatesVerifierTest`（1 文件） | 平台证书自动更新：/v3/certificates 下载→AES-GCM 解密→有效期过滤→替换 | `wx_pay_cert_verifier_test.rs`（10 个测试） | `MIRRORED` | `V1_RUST_LOCAL` | `auto_update_full_flow_with_mock_server` 全流程 MockServer；失败仅告警语义镜像 |
| P07 | `v3/util/RsaCryptoUtilTest`（1 文件） | RSA-OAEP（SHA-1 MGF1）加解密 | `rsa_oaep_roundtrip` | `MIRRORED` | `V1_RUST_LOCAL` | 官方 SDK 向量往返 |
| P08 | `bean` 子域线格式组（11 文件：WxPayApplyment4SubCreateRequest / EntPayRequest / FavorStocksGetResult / BatchDetailsResult / WxPayScoreRequest / WxPartnerPayScoreRequest / ProfitSharingQueryResult / ProfitSharingV3Result / WxPayRedpackQueryResult / WxPaySendRedpackResult / WxSignQueryResult） | 子域 bean 线格式 | — | `NOT_MIRRORED` | `V0_STATIC` | 对应子服务未实现（台账 MISSING：service 子服务接口/实现 58），bean 已生成但无线格式测试镜像 |
| P09 | `config`（3 文件：WxPayConfigTest / WxPayConfigPrivateKeyTest / CustomizedWxPayConfigTest） | 配置字段/私钥 PEM 解析/自定义配置 | — | `NOT_MIRRORED` | `V1_RUST_LOCAL` | PEM/p12 解析经 `v3_crypto`/`cert_verifier` 测试间接覆盖（`load_private_key_from_pem`/`load_p12_*`）；配置 bean 自身无直接镜像 |
| P10 | `service` 与 `service/impl` 子服务测试（25 文件：BusinessOperationTransferServiceTest + Applyment4Sub / BrandMerchantTransfer / BusinessCircle / Complaint / CustomDeclaration / Ecommerce / EntPay / MarketingBusiFavor / MarketingFavor / MarketingMedia / MerchantMedia / MerchantTransfer / MiPay / PartnerPayScoreSignPlan / PartnerTransfer / PayScore / Payroll / ProfitSharing / RealName / Redpack / SubscriptionBilling / Transfer / WxDeposit / WxEntrustPap 的 Impl 测试） | 子服务 HTTP 语义 | — | `NOT_MIRRORED` | `V0_STATIC` | 对应 29 个子服务实现未实现（台账 MISSING）；镜像留待子服务批次 |
| P11 | `service/impl` 门面侧测试（3 文件：MultiAppIdSwitchoverTest / TransferReceiptApiCompatibilityTest / BaseWxPayServiceGlobalImplTest） | 多商户切换/转账回单 API 兼容/境外 v3 全局下单 bean | — | `NOT_MIRRORED` | `V0_STATIC` | 多商户切换语义已实现于 `base_wx_pay_service_impl.rs`（configMap/switchover）但无测试镜像；转账回单与全球单 bean 已生成无镜像 |
| P12 | `v3/auth/AutoUpdateCertificatesVerifierPublicKeyModeTest`（1 文件） | 平台公钥模式（无证书场景）验签 | — | `NOT_MIRRORED` | `V0_STATIC` | 公钥模式未实现（Rust 验签路由以证书序列号为准）；证书模式已镜像（P06） |
| P13 | 基础设施（8 文件：testbase ApiTestModule / CustomizedApiTestModule / XmlWxPayConfig + ConnectionPoolUsageExampleTest / MultiAppIdSwitchoverManualTest / OverseasWxPayExample / WxPayServiceApacheHttpImplConnectionPoolTest / v3 SignatureExecTrustedHostTest） | Guice 注入 + 真实商户号 / Apache 连接池 / 宿主示例 / Apache 执行链受信任主机 | — | `NOT_APPLICABLE` | `V0_STATIC` | JVM DI 框架、Apache HTTP 后端与宿主示例专属（台账 PLATFORM_NA 对应）；受信任主机拦截器为 Apache 执行链专属 |

> 处置汇总：20 `MIRRORED`/`ADAPTED`（P01–P07）+ 43 `NOT_MIRRORED`（P08–P12）+ 8 `NOT_APPLICABLE`（P13）= 71 全部处置。

## 三、`RUST_OBLIGATION`：Rust 特有义务

| ID | Rust 机制/组件 | 必须保护的风险 | 测试与断言 | 层级 | 状态 | 证据 |
|---|---|---|---|---|---|---|
| PR01 | v2 XML 签名报文装配 | 签名错误导致支付失败（参数排序/MD5/HMAC 大写、XML 字段序） | `v2_unified_order_sends_signed_xml`：MockServer 断言 body 含黄金签名、Content-Type text/xml | integration | done | `V1_RUST_LOCAL` |
| PR02 | v2 响应验签与解析 | checkResult 语义（toMap 验签 + return_code/result_code 校验 + 错误文案） | `v2_query_order_xml_and_parse` + `bean/xml.rs` 单元（root_children_map） | integration | done | `V1_RUST_LOCAL` |
| PR03 | p12 证书通道（useKey=true） | SSL 客户端构建失败/密码错误不得静默 | `v2_refund_with_cert_channel_and_sign` + `load_p12_matches_pem_channel`/`load_p12_wrong_password_rejected` | integration | done | `V1_RUST_LOCAL` |
| PR04 | v2 通知验签与解析 | 通知伪造/篡改必须拒绝；SIGNTEST 探测流量识别 | `v2_order_notify_parse_and_sign_verify`；`wx_pay_notify_utils.rs` SIGNTEST 前缀分支（对应 Java WxSignTestException） | integration | done | `V1_RUST_LOCAL` |
| PR05 | v2 退款通知 req_info 解密 | AES-256-ECB 密钥派生（md5(mchKey) 小写）与 Java 一致 | `v2_refund_notify_ecb_decrypt_java_golden`：Java golden 向量 | integration | done | `V1_RUST_LOCAL` |
| PR06 | v3 Authorization 头 | WECHATPAY2-SHA256-RSA2048 格式（时间戳/nonce/body、Wechatpay-Serial） | `v3_create_order_jsapi_authorization_and_pay_info` + `authorization_token_and_header_format` | integration | done | `V1_RUST_LOCAL` |
| PR07 | v3 响应验签 | `timestamp\nnonce\nbody` 消息构造 + SHA256withRSA | `v3_response_verification_rejects_tampered`/`verify_response_signature_flow`/`verify_sha256_rsa` 篡改拒绝 | integration | done | `V1_RUST_LOCAL` |
| PR08 | v3 通知 resource 解密全流程 | 验签→OriginNotifyResponse→AES-GCM 解密→JSON 反序列化 | `v3_order_notify_decrypt_full_flow`（MockServer 全流程） | integration | done | `V1_RUST_LOCAL` |
| PR09 | 对账单/资金账单解析 | Java 原始账单行格式（含 GZIP 通道） | `v2_download_bill_parse_java_format`/`download_fund_flow_with_request_gzip_channel`/`apply_fund_flow_bill_url_and_response` | integration | done | `V1_RUST_LOCAL` |
| PR10 | 平台证书自动更新 | /v3/certificates 下载→AES-GCM 解密→checkValidity 过滤→整体替换；失败仅告警不抛 | `auto_update_full_flow_with_mock_server`/`certificates_response_golden_decrypt`/`certificates_response_skips_expired_entry`/`auto_update_failure_semantics` | integration | done | `V1_RUST_LOCAL` |
| PR11 | 证书验签路由 | 按序列号路由证书公钥（多证书轮换） | `verify_routes_by_serial`/`verify_rejects_tampered`/`auto_verifier_manual_injection_and_verify` | integration | done | `V1_RUST_LOCAL` |
| PR12 | 官方 SDK 算法向量 | AES-GCM 加解密/签名消息/RSA 签名与官方 SDK 字节一致 | `aes_gcm_decrypt_official_sdk_vector`/`aes_gcm_encrypt_official_sdk_vector`/`build_request_message_official_sdk_vector`/`rsa_sign_matches_openssl_golden` | integration | done | `V1_RUST_LOCAL` |
| PR13 | 多商户配置切换（configMap/switchover） | RwLock 切换的并发正确性（Java ThreadLocal 简化形态） | —（实现于 `base_wx_pay_service_impl.rs`，**无测试镜像**，Java MultiAppIdSwitchoverTest 亦未镜像） | integration | ⚠ 未镜像 | `V0_STATIC`（实现）/ 缺测试 |

## 四、`VALUE_ADD`：增量价值测试

| ID | 来源 | 风险/可捕获缺陷 | 新测试 | 与已有测试差异 | 状态 | 证据 |
|---|---|---|---|---|---|---|
| PV01 | 官方 SDK 向量审计 | AES-GCM/SHA256withRSA 与微信官方 SDK 字节级偏差 | `aes_gcm_*_official_sdk_vector`/`build_request_message_official_sdk_vector`/`rsa_sign_matches_openssl_golden`（openssl 生成） | Java 测试未覆盖官方向量 | done | `V1_RUST_LOCAL` |
| PV02 | 证书链路审计 | p12 解析与 PEM 通道不一致/密码错误静默 | `load_p12_matches_pem_channel`/`load_p12_wrong_password_rejected`/`cert_serial_number_official_sdk_certs` | Java 侧无 p12 字节级镜像 | done | `V1_RUST_LOCAL` |
| PV03 | 安全审计 | 篡改密文/签名/证书不得通过 | `aes_gcm_decrypt_tampered_rejected`/`rsa_verify_rejects_tampered`/`certificates_response_rejects_tampered_and_malformed`/`verify_rejects_tampered` | Java 未覆盖拒绝路径 | done | `V1_RUST_LOCAL` |
| PV04 | 生命周期审计 | 过期证书拒绝（checkValidity 与 Java 一致） | `expired_cert_rejected_mirrors_java`/`certificates_response_skips_expired_entry` | 新增（Java 依赖真实证书） | done | `V1_RUST_LOCAL` |
| PV05 | 端到端审计 | v3 通知全链路（验签+解密+解析）在 MockServer 下闭环 | `v3_order_notify_decrypt_full_flow`/`v3_create_order_jsapi_authorization_and_pay_info` | Java 依赖真实商户号 | done | `V1_RUST_LOCAL` |
| PV06 | 字节通道审计 | GZIP 账单字节通道与原始文本解析一致性 | `download_fund_flow_with_request_gzip_channel`/`download_fund_flow_with_request_sends_signed_xml` | Java 未覆盖下载通道 | done | `V1_RUST_LOCAL` |

## 五、差分与规范化

| Case schema/exporter | Java 原始输出 | Rust 原始输出 | normalizer 版本/规则 | 结果 | artifact |
|---|---|---|---|---|---|
| 自动跨语言差分 harness | 未搭建 | — | — | — | — |
| 黄金向量固化：v2 签名（官方文档向量）、v2 退款 req_info AES-ECB 解密（Java golden）、v3 AES-GCM 官方 SDK 向量、RSA 签名（openssl golden）、v3 Authorization 头格式 | 同左 | 同左 | 字节精确 | 一致 | `tests/wx_pay_*` 4 文件 + `bean/xml.rs` 单元 |

## 六、测试价值审计

> 55 个 pay 测试逐一满足：直接调用生产路径、可观察断言、确定性、去重（每个测试独立 MockServer 端口/黄金向量）。

| Rust 测试 | Trace | Observable | Mutation-sensitive | Production path | Deterministic | Distinct | 处置 |
|---|---|---|---|---|---|---|---|
| 55/55（v3_crypto 17、cert_verifier 10、service_impl 17、bean_fix 9、lib 单元 2） | 是 | 是 | 是 | 是 | 是 | 是 | `KEEP` |

## 七、验收结果

| 门禁 | 命令/环境 | 通过/失败/跳过 | 证据级别 | artifact | 未验证边界 |
|---|---|---|---|---|---|
| source disposition（71 个测试对象：20 镜像/适配 + 43 未镜像 + 8 不适用） | 上表 P01–P13 | 通过 | `V0_STATIC`–`V1_RUST_LOCAL` | `docs/migration/weixin-java-pay/迁移测试对照表.md` | Java TestNG 测试未执行；43 个未镜像对象留待子服务批次 |
| Rust obligations（13 项，PR01–PR12 通过、PR13 未镜像） | `cargo test --workspace --all-features` | 通过（546/546，pay 55） | `V1_RUST_LOCAL` | `tests/wx_pay_*.rs`、`src/bean/xml.rs` | PR13 多商户切换无测试镜像 |
| coverage | `cargo llvm-cov --package wx-rust-pay --summary-only` | 通过（pay 40.69% line，TOTAL 42.80%） | `V6_NONFUNCTIONAL`（信号） | `覆盖审计报告.md` | 覆盖率不构成等价证明 |
| 对象台账 | `scripts/audit_pay_ledger.py` | 570/570 处置；**90 MISSING（阻断）** | `V0_STATIC` | `对象级对照表.md` | 90 个未实现对象（子服务 58 + 枚举 30 + 其他 2） |
| real host/rollback | 真实微信支付 API | 跳过 | — | — | V5 集成批次 |

## 八、结论

- 源测试处置率：71/71 全部处置（20 镜像/适配 + 43 未镜像 + 8 不适用）；未镜像 43 项中 25 项
  对应子服务未实现（台账 MISSING），其余为 config/线格式/公钥模式等无镜像项。
- Rust 义务 12/13 通过（签名黄金/证书链路/解密/通知/账单/授权头）；**PR13 多商户切换实现无测试镜像**。
- VALUE_ADD 6 项（官方向量、p12 链路、篡改拒绝、过期证书、端到端、GZIP 通道）。
- **对象缺口 90 个（子服务接口 29 + 子服务实现 29 + bean 枚举 30 + WxPayErrorCode + ReceiverList）如实列出**，
  阻断"全量交付"结论；已实现面（bean + 门面 + v3 签名/证书/通知 utils）测试全绿。
- 模块状态：⚠️ **部分交付**（546 workspace tests 全绿，pay 55；台账 90 MISSING 阻断）。
