# weixin-java-mp 覆盖审计报告（B2 批次 2 全量交付：核心层 + 子域 + 109 批次）

> 审计日期：2026-08-01
> Java 基线：`a49d6e1461752c06b752d2afd8aeeb7e6e78cefe`
> Rust 基线：`2026-08-01 working-tree（crates/wx-rust-mp，386 个对象文件 + 6 个测试文件；台账 428 对象全部处置）`
> 测试命令：`cargo test --workspace --all-features`（153/153，mp 70）
> 覆盖命令：`cargo llvm-cov --workspace --all-features --summary-only`
> 配套文档：`迁移测试对照表.md`、`对象级对照表.md`（全部 428 对象处置）

## 一、SOURCE_PARITY 处置覆盖（本批次）

| 维度 | 数值 |
|---|---|
| Java 测试对象（模块） | 71 |
| 本批次已处置 | 15（9 `MIRRORED` + 3 `ADAPTED` + 3 组 `NOT_APPLICABLE`） |
| 留待子域批次 | 56（card/guide/marketing/draft 等子域测试对象） |
| Java 测试执行 | 未运行（TestNG；Maven surefire 默认不执行） |

**处置依据**：`NOT_APPLICABLE` 对应台账 `MISSING` 子域对象；本批次聚焦
门面/配置/消息路由/XML 消息/核心 bean + 5 子服务（menu/template/qrcode/kefu/user）。

## 二、行为证据层级（V0–V4）

| 层级 | 覆盖对象 | 证据位置 |
|---|---|---|
| V0_STATIC | 结构/URL 常量/线格式 | `wx_mp_api_url.rs`（22.58%，URL 常量表按需触达）；62 对象文件结构 |
| V1_RUST_LOCAL | 44 个 mp 测试 | `tests/` 4 文件：XML golden、路由 10 用例、模板截断、kefu 线格式、jsapi 黄金向量 |
| V2_GOLDEN | 跨语言黄金（Java 夹具派生） | jsapi `c6f04b64…`、模板 JSON 逐字节、out 消息 XML、kefu `touser`/`msgtype` |
| V3/V4 | 未做 | — |

## 三、Rust 义务完成度

17 项义务测试全部通过（见对照表 MR01–MR17），关键项：
- **修复类**：消息加密签名算法（common 原用 concat 拼接，微信线格式要求排序后 `&` 连接——修复后与 Java `SHA1.gen` 兼容）；XML 解析器（递归 Start 消费、单元素数组、Eof 处理）
- **并发**：token 双检锁 guard 持有（曾现 `break true` 丢锁 bug）、ticket 按类型锁、多公众号切换
- **执行引擎**：-1 指数退避、token 过期单次自动刷新（flag 循环防递归）
- **安全/编码**：签名错误拒绝、畸形 XML 拒绝、RFC 3986、Unicode 截断

## 四、VALUE_ADD

6 项（MV01–MV06）：mock HTTP 服务器驱动的执行引擎测试（Java 无此基础设施）、
并发单飞、错误签名拒绝、URI 编码、多公众号 token 隔离。

## 五、覆盖率与可比 scope

| 指标 | 值 | 说明 |
|---|---|---|
| workspace line | **43.34%**（5265/12147） | 全量交付后线覆盖；服务 impl 7–70% |
| workspace fn | **36.62%**（621/1696） | 全量交付后 fn 覆盖 |
| workspace region | **36.74%** | — |

**mp 核心文件（关键语义面）**：

| 文件 | line | 说明 |
|---|---:|---|
| `wx_mp_xml_message.rs` | **85.77%** | XML 全消息模型（最大语义面） |
| `wx_mp_message_router.rs` | **84.19%** | 路由收集/同步/异步/去重 |
| `wx_mp_message_router_rule.rs` | **83.65%** | 规则匹配/处理器链 |
| `wx_mp_template_message.rs` | **92.26%** | 模板消息 + 截断 |
| `wx_mp_default_config_impl.rs` | **83.33%** | 配置存储 |
| `wx_mp_xml_out_message.rs` | **79.08%** | out 消息 XML |
| `wx_mp_menu.rs` | **90.91%** | 菜单 |
| `wx_mp_service_impl.rs` | **63.51%** | 门面实现（多配置/子服务） |
| `wx_mp_service.rs` | **51.81%** | 门面默认实现（未测 API 方法：get_callback_ip/net_check/semantic_query 等需真实/扩展 mock） |
| `wx_mp_api_url.rs` | 22.58% | URL 常量表（按需触达，非行为面） |

**结构性低覆盖**：40 个文件 0%——message 子 bean（send_pics_info 等 6 个，解析经 xml_message 覆盖但结构体自身未直接测）、
result bean 5 个（from_json 未测）、部分 builder（build() 链仅 text 测）、trait 文件（config_storage/menu_service 等接口）、
以及全部未实现子域占位（无）。

**覆盖率不是等价证明**——mp 44 个测试聚焦核心语义面（XML/路由/token/模板），
子域 API（36 个服务接口）未测；覆盖率仅作为本批次核心层信号。

## 六、缺口与后续

| 缺口 | 类型 | 处置 |
|---|---|---|
| 366 个对象 `MISSING`（card/guide/marketing/draft/freepublish/dataCube/device/shake/wifi/comment/memberCard/massMessage/aiOpen/ocr/imgProc/reimburseInvoice/store/userTag/userBlacklist/…） | 对象 | 按路线图子域批次 |
| 门面 20+ 通用 API 方法（callbackIP/netCheck/semantic/autoReply/clearQuota/shorten） | 行为 | 扩展 mock 服务器覆盖 |
| 子服务 HTTP 集成（menu_create 已测；template/qrcode/kefu/user 的请求体 golden） | 行为 | 下一子批次 |
| Java TestNG 测试执行 | 工具链 | JDK 8 + TestNG 配置 |
| V5 真实微信 API 宿主 | 宿主 | 集成批次 |

## 七、结论

- 核心层源测试 15/15 处置；高风险合同（XML/路由/token/模板/kefu/jsapi）均有 Java 派生 oracle
- 义务 17/17 通过（含 2 个实现修复）；VALUE_ADD 6 项
- 覆盖率 43.88%（workspace）作为**信号**；mp 核心语义文件 ≥79%
- **366 个 `MISSING` 阻断模块完成**——本批次为核心层交付，按路线图继续
