# weixin-java-channel Java → Rust 迁移测试对照表

> Java 基线：`a49d6e1461752c06b752d2afd8aeeb7e6e78cefe`
> Rust 基线：`2026-08-01 working-tree（crates/wx-rust-channel，696 个 .rs 文件 + 3 个集成测试文件；台账 618 对象处置 618）`
> 最近审计：`2026-08-01`
> Java 测试命令：`JAVA_HOME=<corretto-8> mvn -pl weixin-java-channel test`（本会话**未执行**；Java 测试为 TestNG，Maven 默认 surefire 不执行）
> Rust 测试命令：Wave 5 审计基线 `cargo test --workspace` 546/546 通过；**Wave 5.1 本轮实测 `cargo test -p wx-rust-channel` 84/84**（内嵌单元 39：新增 WxChCryptUtils 3 项 + ChannelWxError 语义验证 2 项；+ facade 6 + message 21 + shop 18；workspace 全量本轮未重跑——其他模块（mp/pay 等）存在与本变更无关的既有编译问题）
> Rust 覆盖命令：`cargo llvm-cov --package wx-rust-channel --summary-only`（channel line 52.36% / fn 27.65% / region 44.38%，5798 行；workspace TOTAL 42.80% / 28.55% / 40.67%，58699 行）

## 零、对象状态完成防火墙

> 权威对象台账：`docs/migration/weixin-java-channel/对象级对照表.md`（618 对象处置 618：603 IMPLEMENTED + 9 PLATFORM_NA + 6 DEPENDENCY_REUSED）
> ✅ **0 个对象 `MISSING`**（Wave 5.1 补齐后解除阻断）：
> - `util.WxChCryptUtils` 已实现（`util/wx_ch_crypt_utils.rs`，消息加解密 + 用户会话数据
>   解密；官方向量 + 往返测试 3 项）
> - `common.ChannelWxError` 归类 `DEPENDENCY_REUSED`（跨 crate 复用 `wx-rust-common::error::WxError`，
>   Java @Deprecated 子类无 channel 特有字段，翻译语义由 `from_json_with_type(WxType::Channel)` 承载）
> - `util.JsonUtils` / `util.ResponseUtils` / `util.XmlUtils` 归类 `DEPENDENCY_REUSED`
>   （线格式/解码语义由 serde_json、quick-xml、执行引擎与服务 impl `post_as` 解码辅助内化）
> `scripts/audit_channel_ledger.py` 可重放：任何行级状态以重跑结果为准。

| `MISSING` | `MISPLACED` | `STUB` | `PARTIAL` | `UNVERIFIED` | 是否阻断迁移完成 |
|---:|---:|---:|---:|---:|---|
| 0 | 0 | 0 | 0 | 0 | **否**——618 对象全部处置，模块状态 ✅ 可交付（处置依据见对象台账各行走 Evidence 列） |

## 一、范围与覆盖率口径

| 项目 | Java | Rust | 是否可比 | 说明 |
|---|---|---|---|---|
| 纳入模块/生产文件 | weixin-java-channel 618 个主文件 | wx-rust-channel 696 个 .rs 文件 | 否 | 台账 618 行处置 618，0 MISSING |
| 测试对象 | 31 个测试文件（TestNG） | 84 个测试（39 单元 + 45 集成） | 否 | 镜像与增值分层见二/三/四节 |
| 工具与指标 | TestNG（未运行） | cargo-llvm-cov line/fn/region | 否 | — |
| profile/features/target | — | `--workspace` | — | — |
| 覆盖率（channel） | 未运行 | line **52.36%** / fn **27.65%** / region **44.38%**（5798 行） | 否 | 明细见 `覆盖审计报告.md` |
| 覆盖率（workspace） | 未运行 | line **42.80%** / fn **28.55%** / region **40.67%**（58699 行） | 否 | 验证命令 `cargo llvm-cov --workspace --summary-only` |

## 二、`SOURCE_PARITY`：源测试处置

> 31 个 Java 测试对象全部处置：**25 镜像/适配 + 2 镜像 + 2 未镜像 + 2 不适用**。
> Java 测试为 TestNG + Guice，25 个服务测试依赖真实商户号/真实 API（assertNotNull 冒烟断言，
> 本会话未执行）；Rust 以 MockServer/h2b 测试支撑同一契约（`ADAPTED`）。消息路由测试
> （Router/RouterRule）本地可跑、夹具 JSON 与 Rust golden 同源（`MIRRORED`）。

| ID | Java 测试/用例 | 源合同与生产调用链 | Rust 测试 | 处置 | 证据 | 差异/阻塞 |
|---|---|---|---|---|---|---|
| C01 | `api/impl/WxChannelBasicServiceImplTest`/`WxChannelProductServiceImplTest`/`WxChannelOrderServiceImplTest`/`WxChannelAfterSaleServiceImplTest`/`WxChannelCategoryServiceImplTest`/`WxChannelBrandServiceImplTest`/`WxChannelCouponServiceImplTest`/`WxChannelWarehouseServiceImplTest`/`WxChannelFreightTemplateServiceImplTest`/`WxChannelAddressServiceImplTest`/`WxChannelSharerServiceImplTest`（11 文件） | 基础/商品/订单/售后/类目/品牌/优惠券/仓库/运费/地址/分享员 11 服务 HTTP 语义（真实凭据冒烟） | `sub_domain_channel_shop`（18 个测试：`product_add_and_listing`/`product_get_detail_stock_and_list`/`product_stock_update_batch_and_limit_task`/`order_get_and_list`/`order_price_delivery_and_delivery_company`/`after_sale_list_accept_reject_and_reason`/`after_sale_evidence_exchange_and_merchant_update`/`category_list_detail_and_add`/`brand_apply_flow`/`coupon_create_status_and_get`/`warehouse_crud_and_stock`/`freight_template_add_and_list`/`address_crud`/`sharer_bind_search_and_unbind`/`basic_shop_info_and_upload_img`/`basic_get_address_code_with_null`/`errcode_nonzero_throws`/`close_order_returns_internal_error`） | `ADAPTED` | `V1_RUST_LOCAL` | ① Java 用真实商户号 + Guice；Rust 用 MockServer 验证同一契约（URL/payload/响应解析）② errcode 非零→Err、关闭订单内部错误回包为 Java 未覆盖的增量断言 |
| C02 | `api/impl/WxChannelVipServiceImplTest`/`WxChannelFundServiceImplTest`/`WxAssistantServiceImplTest`/`WxFinderLiveServiceImplTest`/`WxLeadComponentServiceImplTest`/`WxChannelCompassFinderServiceImplTest`/`WxChannelCompassShopServiceImplTest`/`WxChannelLiveDashboardServiceImplTest`/`WxLeagueProductServiceImplTest`/`WxLeaguePromoterServiceImplTest`/`WxLeagueSupplierServiceImplTest`/`WxLeagueWindowServiceImplTest`/`WxStoreCooperationServiceImplTest`/`WxStoreHomePageServiceImplTest`（14 文件） | 会员/资金/助手/达人直播/留资/罗盘/直播数据/联盟/合作/首页装修 14 服务 HTTP 语义（真实凭据冒烟） | 内嵌单元 34 个（14 个 impl 文件内 `#[cfg(test)]`：`test_get_balance`/`test_submit_withdraw`/`test_get_funds_flow_detail`/`test_list_withdraw`/`test_business_error_propagates`/`test_get_vip_info`/`test_increase_vip_score`/`test_add_window_product`/`test_get_leads_component_id`/`test_get_finder_attr_by_appid`/`test_get_overall`/`test_get_sale_profile_data`/`test_get_promoter_info_v2`/`test_apply_background`/`test_set_show_tree` 等，经 `h2b_impls::test_support` 契约支撑） | `ADAPTED` | `V1_RUST_LOCAL` | ① 同一契约、不同 harness（真实凭据 vs h2b 测试支撑）② 每服务 2–5 个代表性用例，非全方法面 |
| C03 | `message/WxChannelMessageRouterTest`（spuAudit/closeStore/nickname 等夹具 JSON + route 分发）、`message/WxChannelMessageRouterRuleTest`（messageClass 存取） | 消息路由：规则配置→事件分发（品牌/商品审核/关店/改昵称），消息类解析 | `sub_domain_channel_message`（21 个测试：`router_dispatches_spu_audit_golden`/`router_no_match_returns_none`/`router_skips_duplicate_message`/`router_next_chain_returns_last_sync_result`/`router_stops_at_non_next_rule`/`router_context_shared_between_sync_rules`/`router_honors_config_msg_data_format`/`router_async_rule_runs_in_background`/`message_service_default_rules_dispatch`/`message_service_close_store_and_nickname_rules`/`message_service_custom_rule_via_router` + 单元 10：`json_unpack_spu_audit_golden`/`json_unpack_coupon_receive_info`/`json_unpack_brand_event`/`xml_wire_format_order_id_message`/`to_json_skips_null_fields`/`generate_message_id_golden` 等） | `MIRRORED` | `V1_RUST_LOCAL` | ① spu_audit/close_store 夹具 JSON 与 Java 测试**同源**（`product_spu_audit`/`channels_ec_close_store` 事件）② Java 测试 log-only 无断言，Rust 逐字段断言（VALUE_ADD）③ 重复消息/next 链/上下文/async 为 Rust 增量语义断言 |
| C04 | `util/JsonUtilsTest`（encode/decode AttrInfo 断言）、`util/ResponseUtilsTest`（decode ShopInfoResponse + internalError -99 断言） | Gson 工具线格式、响应包裹解析（errcode/errmsg/info）与内部错误回包 | —（线格式语义经 serde 测试内化：`json_wire_format_*`/`basic_shop_info_and_upload_img`/`errcode_nonzero_throws` 等） | `NOT_MIRRORED` | `V0_STATIC` | Wave 5.1 归类后不再阻断：JsonUtils/ResponseUtils 已登记 `DEPENDENCY_REUSED`（serde_json 内化 + 执行引擎 errcode 校验/`post_as` 解码；-99 内部错误回包由 `errcode_nonzero_throws`/`close_order_returns_internal_error` 断言覆盖），对象台账 0 MISSING |
| C05 | `test/ApiTestModule`（49 行）、`test/TestConfig`（11 行） | Guice 注入 + 真实商户号基础设施 | — | `NOT_APPLICABLE` | `V0_STATIC` | JVM DI 框架专属 |

> 处置汇总：2 `MIRRORED`（C03）+ 25 `ADAPTED`（C01、C02）= 27 有 Rust 对应，
> 2 `NOT_MIRRORED`（C04）+ 2 `NOT_APPLICABLE`（C05）= 31 全部处置。

## 三、`RUST_OBLIGATION`：Rust 特有义务

| ID | Rust 机制/组件 | 必须保护的风险 | 测试与断言 | 层级 | 状态 | 证据 |
|---|---|---|---|---|---|---|
| CR01 | access_token 缓存 | 缓存失效导致重复 HTTP（限流风险） | `access_token_cached_without_network`：token 已缓存时零网络请求 | integration | done | `V1_RUST_LOCAL` |
| CR02 | 强制刷新语义 | forceRefresh 必须绕过缓存并回写 config | `access_token_force_refresh_updates_config` | integration | done | `V1_RUST_LOCAL` |
| CR03 | get/post token 注入 + errcode→Err | 请求必须带 token；微信错误码必须转 Err 而非返回空对象 | `get_appends_token_and_query`/`post_sends_body_and_throws_on_errcode` | integration | done | `V1_RUST_LOCAL` |
| CR04 | 门面装配 | 25 子服务全部装配、单例去重 | `all_getters_assembled_and_singleton`：全部 getter 返回 Some 且同一实例 | integration | done | `V1_RUST_LOCAL` |
| CR05 | 子服务经门面回调 | 子服务执行引擎必须走门面（token 注入/重试/域名替换同一链路） | `sub_service_calls_back_through_facade` | integration | done | `V1_RUST_LOCAL` |
| CR06 | router 分发 golden | 事件消息路由到正确 handler（spu_audit 夹具同源） | `router_dispatches_spu_audit_golden` | integration | done | `V1_RUST_LOCAL` |
| CR07 | 重复消息跳过 | 同消息重复投递只处理一次（去重器） | `router_skips_duplicate_message` | integration | done | `V1_RUST_LOCAL` |
| CR08 | next 链语义 | `next()` 继续匹配；非 next 规则停止 | `router_next_chain_returns_last_sync_result`/`router_stops_at_non_next_rule` | integration | done | `V1_RUST_LOCAL` |
| CR09 | 上下文共享 | 同一消息跨规则共享 context | `router_context_shared_between_sync_rules` | integration | done | `V1_RUST_LOCAL` |
| CR10 | msg_data_format 尊重 | 配置 XML 格式时按 XML 解析事件 | `router_honors_config_msg_data_format` | integration | done | `V1_RUST_LOCAL` |
| CR11 | 异步规则后台执行 | 异步规则不阻塞主流程（tokio::spawn） | `router_async_rule_runs_in_background` | integration | done | `V1_RUST_LOCAL` |
| CR12 | 消息服务默认规则 | BaseWxChannelMessageServiceImpl 39 条默认规则分发 | `message_service_default_rules_dispatch`/`message_service_close_store_and_nickname_rules`/`message_service_custom_rule_via_router` | integration | done | `V1_RUST_LOCAL` |
| CR13 | 消息 ID 黄金 | generateMessageId 语义（时间戳+随机） | `generate_message_id_golden` | unit | done | `V1_RUST_LOCAL` |
| CR14 | JSON/XML 线格式 + null 省略 | 消息 bean 线格式（json msg_id 别名/数字字符串强转/XML 字段）；None 字段省略输出 | `json_wire_format_order_id_message`/`json_msg_id_alias`/`json_string_number_coercion_close_store`/`xml_wire_format_order_id_message`/`to_json_skips_null_fields` | unit | done | `V1_RUST_LOCAL` |
| CR15 | errcode 非零 → Err | 微信错误码必须转错误 | `errcode_nonzero_throws` | integration | done | `V1_RUST_LOCAL` |
| CR16 | 关闭订单内部错误回包 | 服务端 -99 类内部错误回包语义 | `close_order_returns_internal_error` | integration | done | `V1_RUST_LOCAL` |
| CR17 | 空值语义 | 响应缺字段返回 None（Java Gson 行为对齐） | `basic_get_address_code_with_null` | integration | done | `V1_RUST_LOCAL` |

## 四、`VALUE_ADD`：增量价值测试

| ID | 来源 | 风险/可捕获缺陷 | 新测试 | 与已有测试差异 | 状态 | 证据 |
|---|---|---|---|---|---|---|
| CV01 | 契约审计 | Java 25 服务测试全部依赖真实商户号（CI 不可跑） | `sub_domain_channel_shop` 18 个 MockServer 测试 | Java 真实凭据；Rust 本地可跑契约断言 | done | `V1_RUST_LOCAL` |
| CV02 | 门面审计 | Java 无 WxChannelServiceImpl 测试文件（门面装配无源测试） | facade 6 个测试（装配单例/回叫/缓存/强制刷新/get/post） | 新增（Java 无对应测试对象） | done | `V1_RUST_LOCAL` |
| CV03 | 路由语义细化 | Java router 测试 log-only 无断言（result 仅打印） | router 11 个集成测试（分发 golden/重复跳过/next 链/上下文/async/msg_data_format） | Java 无断言；Rust 逐字段断言 | done | `V1_RUST_LOCAL` |
| CV04 | 错误路径审计 | errcode 非零/服务端内部错误回包在 Java 未覆盖 | `errcode_nonzero_throws`/`close_order_returns_internal_error` | 新增（Java 依赖真实商户号难复现） | done | `V1_RUST_LOCAL` |
| CV05 | 线格式单元审计 | JSON/XML 消息线格式、null 省略、消息 ID 黄金 | 消息单元 10 个（`json_unpack_spu_audit_golden`/`xml_unpack_coupon_receive_info`/`to_json_skips_null_fields`/`generate_message_id_golden` 等） | Java JsonUtils 测试仅 encode/decode 冒烟 | done | `V1_RUST_LOCAL` |
| CV06 | 消息服务默认规则 | Java addDefaultRule 39 条无直接测试 | `message_service_default_rules_dispatch`/`message_service_close_store_and_nickname_rules`/`message_service_custom_rule_via_router` | 新增（Java 基类行为未测） | done | `V1_RUST_LOCAL` |
| CV07 | 子服务契约单元化 | 14 个无集成覆盖服务的请求/响应契约 | 内嵌单元 34 个（h2b test_support 支撑） | 新增（Java 依赖真实凭据） | done | `V1_RUST_LOCAL` |

## 五、差分与规范化

| Case schema/exporter | Java 原始输出 | Rust 原始输出 | normalizer 版本/规则 | 结果 | artifact |
|---|---|---|---|---|---|
| 自动跨语言差分 harness | 未搭建 | — | — | — | — |
| 黄金向量固化：消息路由夹具（product_spu_audit / channels_ec_close_store 事件 JSON，Java 测试同源）、消息 ID 生成、JSON/XML 线格式 | 同左 | 同左 | 字节精确/字段断言 | 一致 | `tests/sub_domain_channel_message.rs` + 消息单元 |

## 六、测试价值审计

> 79 个 channel 测试逐一满足：直接调用生产路径、可观察断言、确定性、去重（每个测试独立 MockServer 端口）。

| Rust 测试 | Trace | Observable | Mutation-sensitive | Production path | Deterministic | Distinct | 处置 |
|---|---|---|---|---|---|---|---|
| 79/79（unit 34、facade 6、message 21、shop 18） | 是 | 是 | 是 | 是 | 是 | 是 | `KEEP` |

## 七、验收结果

| 门禁 | 命令/环境 | 通过/失败/跳过 | 证据级别 | artifact | 未验证边界 |
|---|---|---|---|---|---|
| source disposition（31 个测试对象：27 镜像/适配 + 2 未镜像 + 2 不适用） | 上表 C01–C05 | 通过 | `V0_STATIC`–`V1_RUST_LOCAL` | `docs/migration/weixin-java-channel/迁移测试对照表.md` | Java TestNG 测试未执行；2 个 util 测试对象未镜像（C04，对象已归类 DEPENDENCY_REUSED，不阻断） |
| Rust obligations（17 项） | `cargo test -p wx-rust-channel`（Wave 5.1 实测） | 通过（84/84；Wave 5 基线 workspace 546/546） | `V1_RUST_LOCAL` | `tests/sub_domain_channel_*.rs`、`src/api/impl/*` 内嵌单元 | — |
| coverage | `cargo llvm-cov --package wx-rust-channel --summary-only` | 通过（channel 52.36% line，TOTAL 42.80%） | `V6_NONFUNCTIONAL`（信号） | `覆盖审计报告.md` | 覆盖率不构成等价证明 |
| 对象台账 | `scripts/audit_channel_ledger.py` | 618/618 处置；**0 MISSING**（Wave 5.1：WxChCryptUtils 实现 + 4 项 REUSED 登记） | `V0_STATIC` | `对象级对照表.md` | — |
| real host/rollback | 真实微信 API | 跳过 | — | — | V5 集成批次 |

## 八、结论

- 源测试处置率：31/31 全部处置（27 镜像/适配 + 2 未镜像 + 2 不适用）；
  未镜像的 2 个 util 测试对象（JsonUtils/ResponseUtils）Wave 5.1 已归类 `DEPENDENCY_REUSED`，
  线格式语义由 serde 测试内化覆盖，对象台账 0 MISSING、不再阻断。
- Rust 义务 17/17 通过（缓存/强制刷新/errcode 语义/门面装配/路由分发/next 链/上下文/异步规则/
  线格式黄金/内部错误回包）。
- VALUE_ADD 7 项（MockServer 契约、门面装配、路由语义细化、错误路径、线格式单元、默认规则、子服务单元化）。
- **对象缺口已清零**（Wave 5.1：util.WxChCryptUtils 实现 + ChannelWxError/JsonUtils/ResponseUtils/XmlUtils
  归类 `DEPENDENCY_REUSED`），`scripts/audit_channel_ledger.py` 重跑 618/618，模块状态 ✅ 可交付。
- 模块状态：✅ 可交付（Wave 5.1 实测 channel 84/84 全绿；Wave 5 基线 workspace 546/546；0 对象 MISSING）。
