# weixin-java-open Java → Rust 迁移测试对照表

> Java 基线：`a49d6e1461752c06b752d2afd8aeeb7e6e78cefe`
> Rust 基线：`2026-08-01 working-tree（crates/wx-rust-open，239 个 .rs 文件 + 3 个集成测试文件；台账 240 对象处置 240）`
> 最近审计：`2026-08-01`
> Java 测试命令：`JAVA_HOME=<corretto-8> mvn -pl weixin-java-open test`（本会话**未执行**；Java 测试为 TestNG，Maven 默认 surefire 不执行）
> Rust 测试命令：`cargo test -p wx-rust-open`（**47/47 通过**：`tests/wx_open_component_service_test.rs` 19 + `tests/wx_open_mp_ma_bridge_test.rs` 12 + `tests/wx_open_ma_domain_test.rs` 13 + lib 内嵌单元 3；workspace 其余模块的既有失败与 open 无关）
> Rust 覆盖命令：`cargo llvm-cov --package wx-rust-open --summary-only`（open line 44.97% / fn 31.46% / region 44.26%，3229 行；workspace TOTAL 42.80% / 28.55% / 40.67%，58699 行）

## 零、对象状态完成防火墙

> 权威对象台账：`docs/migration/weixin-java-open/对象级对照表.md`（240 对象处置 240：213 IMPLEMENTED + 14 PLATFORM_NA + 13 DEPENDENCY_REUSED）
> ✅ **0 个对象 `MISSING`**（Wave 6 O5 补齐：Ma*/Minishop 子域接口 9 + 实现 11 + PrivacyKeyEnum 1 全部翻转 IMPLEMENTED，
> 见 `对象级对照表.md` 各行证据；`scripts/audit_open_ledger.py` 可重放复核）。

| `MISSING` | `MISPLACED` | `STUB` | `PARTIAL` | `UNVERIFIED` | 是否阻断迁移完成 |
|---:|---:|---:|---:|---:|---|
| 0 | 0 | 0 | 0 | 0 | **否**——21 个 MISSING 已全部补齐（Wave 6 O5），模块状态 ✅ 全量交付 |

## 一、范围与覆盖率口径

| 项目 | Java | Rust | 是否可比 | 说明 |
|---|---|---|---|---|
| 纳入模块/生产文件 | weixin-java-open 240 个主文件 | wx-rust-open 239 个 .rs 文件 | 否 | 台账 240 行处置 240，0 MISSING |
| 测试对象 | 13 个测试文件（TestNG） | 47 个测试（19 集成 + 12 集成 + 13 集成 + 3 单元） | 否 | 镜像与增值分层见二/三/四节 |
| 工具与指标 | TestNG（未运行） | cargo-llvm-cov line/fn/region | 否 | — |
| profile/features/target | — | `--workspace` | — | — |
| 覆盖率（open） | 未运行 | line **44.97%** / fn **31.46%** / region **44.26%**（3229 行） | 否 | 明细见 `覆盖审计报告.md` |
| 覆盖率（workspace） | 未运行 | line **42.80%** / fn **28.55%** / region **40.67%**（58699 行） | 否 | 验证命令 `cargo llvm-cov --workspace --summary-only` |

## 二、`SOURCE_PARITY`：源测试处置

> 13 个 Java 测试对象全部处置：**0 镜像/适配 + 3 未镜像 + 10 不适用**。
> 如实说明：open 模块 Java 测试绝大多数是**空桩**（WxOpenComponentServiceImplTest 37/37、
> WxOpenFastMaServiceImplTest 52/52、WxOpenMaServiceImplTest 80/80 无方法体，仅 3 个需真实凭据的注释演示），
> 无断言可镜像；Rust 的 47 个测试是按 **Java 实现语义**（服务 impl 与组件契约）新建的 MockServer 契约测试
> （计入 RUST_OBLIGATION/VALUE_ADD），并非源测试镜像。Java 测试为 TestNG，本会话未执行。

| ID | Java 测试/用例 | 源合同与生产调用链 | Rust 测试 | 处置 | 证据 | 差异/阻塞 |
|---|---|---|---|---|---|---|
| O01 | `api/impl/WxOpenComponentServiceImplTest`（37 个 `@Test`，**全部空桩**） | 组件服务表面：getWxMp/Ma/FastMaServiceByAppid/checkSignature/component_access_token/get/post/pre_auth_url/route/getQueryAuth/授权方信息/模板/open account/oauth2/jscode2session 等 | component 19 个测试（`get_query_auth_writes_authorizer_tokens`/`route_*` 5/`get_pre_auth_code_and_url_build`/`authorizer_access_token_refresh_chain`/`component_token_auto_refresh_on_40001`/`get_template_list_parses`/`upload_minishop_media_file_multipart`/`get_minishop_categories_manual_parse`/`oauth2_get_access_token_parses`/`get_authorizer_list_writes_refresh_tokens`/`minishop_create_coupon_returns_id` 等） | `NOT_APPLICABLE` | `V1_RUST_LOCAL` | Java 测试无断言空桩；同契约由 Rust MockServer 测试承载（VALUE_ADD OV02） |
| O02 | `api/impl/WxOpenFastMaServiceImplTest`（52 个 `@Test`，**全部空桩**） | 快速创建小程序服务表面（Java 类 @Deprecated，2021-06-23 起以 WxOpenMaService 替代） | bridge 12 个测试（token 委托/缓存按 appid 分桶/jscode2session 委托/open account 创建绑定解绑） | `NOT_APPLICABLE` | `V1_RUST_LOCAL` | 无断言空桩；快速创建语义由 bridge 测试承载（`create_open_account_mp_request_and_result`/`bind_open_account_mini_returns_true`/`unbind_open_account_returns_false_without_errcode`） |
| O03 | `api/impl/WxOpenMaServiceImplTest`（83 个 `@Test`：80 空桩 + 3 注释演示，需真实凭据） | 代 ma 服务表面（token 链、jsCode2Session、模板/审核/额度查询等） | bridge 12 个测试（`bridge_access_token_reads_open_authorizer_cache`/`bridge_ma_jscode2session_delegates_component`/`bridge_ma_post_injects_authorizer_token`/`bridge_services_cached_per_appid_and_independent`/`get_ma_and_fast_ma_services_return_some_and_downcast`） | `NOT_APPLICABLE` | `V1_RUST_LOCAL` | 无断言空桩 + 注释演示；桥接语义（token 委托链/注入/缓存）由 Rust bridge 测试覆盖 |
| O04 | `api/impl/WxOpenOAuth2ServiceImplTest`（冒烟调用，**0 断言**） | WxOpenOAuth2ServiceImpl（extends WxOpenServiceImpl implements WxOAuth2Service）buildAuthorizationUrl/getAccessToken/refreshAccessToken/getUserInfo/validateAccessToken | — | `NOT_APPLICABLE` | `V0_STATIC` | 对象为台账 **MISSING**（Rust 无 WxOAuth2Service 实现）；Java 测试本身无断言 |
| O05 | `api/impl/WxOpenMpOAuth2ServiceImplTest`（冒烟调用，**0 断言**） | WxOpenMpOAuth2ServiceImpl（extends WxOAuth2ServiceDecorator）component URL 版 getAccessToken/buildAuthorizationUrl | `oauth2_get_access_token_parses`（component service `oauth2_get_access_token` 解析） | `NOT_APPLICABLE` | `V1_RUST_LOCAL` | 对象为台账 **MISSING**（部分镜像：access/refresh token 内联 component service，buildAuthorizationUrl 组件 appid 拼装未实现）；Java 测试无断言 |
| O06 | `api/impl/WxOpenInRedisConfigStorageTest`（9 个用例，18 断言） | Redis 外部存储配置（Jedis） | — | `NOT_APPLICABLE` | `V0_STATIC` | Redis 外部存储专属（台账 PLATFORM_NA 对应）；接入留待宿主集成 |
| O07 | `api/impl/WxOpenInRedissonConfigStorageTest`（9 个用例，18 断言） | Redisson 外部存储配置 | — | `NOT_APPLICABLE` | `V0_STATIC` | Redisson 外部存储专属（台账 PLATFORM_NA 对应）；接入留待宿主集成 |
| O08 | `bean/result/WxFastMaAccountBasicInfoResultTest`（fromJson 10 断言） | 快速创建小程序账号基本信息线格式 | — | `NOT_MIRRORED` | `V0_STATIC` | bean 已实现（`bean/result/wx_fast_ma_account_basic_info_result.rs`）但无 Rust 测试触达；镜像留待补齐 |
| O09 | `bean/result/WxFastMaBeenSetCategoryResultTest`（fromJson 6 断言） | 已设置类目线格式 | — | `NOT_MIRRORED` | `V0_STATIC` | bean 已实现（`wx_fast_ma_been_set_category_result.rs`）但无 Rust 测试触达 |
| O10 | `bean/result/WxFastMaCanSetCategoryResultTest`（fromJson 3 断言） | 可设置类目线格式 | — | `NOT_MIRRORED` | `V0_STATIC` | bean 已实现（`wx_fast_ma_can_set_category_result.rs`）但无 Rust 测试触达 |
| O11 | `bean/result/WxOpenAuthorizerInfoResultTest`（**无断言**，System.out 打印调试） | 授权方信息 JSON 反序列化（snake_case 键 + func_info 扁平化 + MiniProgramInfo 大驼峰） | `authorizer_info_parses_java_golden`（src/bean/result 单元 + component 集成双份，同夹具同源断言） | `NOT_APPLICABLE` | `V1_RUST_LOCAL` | Java 用例本身无断言（调试打印）；其夹具由 Rust golden 断言复用（VALUE_ADD OV01） |
| O12 | `test/ApiTestModule`（66 行） | Guice 注入 + 真实测试号基础设施 | — | `NOT_APPLICABLE` | `V0_STATIC` | JVM DI 框架专属 |
| O13 | `test/TestConfigStorage`（16 行） | 测试配置存储（真实组件凭证） | — | `NOT_APPLICABLE` | `V0_STATIC` | 测试基础设施专属 |

> 处置汇总：0 `MIRRORED` + 0 `ADAPTED`，3 `NOT_MIRRORED`（O08–O10）+ 10 `NOT_APPLICABLE`（O01–O07、O11–O13）= 13 全部处置。
> 注：O01–O05 的**合同覆盖**不因此为空——Rust 按 Java impl 语义新建 47 个测试（Wave 6 增补 Ma 子域 13 个，见三/四节），
> 这与「Java 测试对象不可镜像（空桩/无断言）」是两件事，分别记账。

## 三、`RUST_OBLIGATION`：Rust 特有义务

| ID | Rust 机制/组件 | 必须保护的风险 | 测试与断言 | 层级 | 状态 | 证据 |
|---|---|---|---|---|---|---|
| OR01 | component_access_token 双检锁刷新链 | 并发下重复刷新（限流风险）、40001 后自动续期 | `component_token_auto_refresh_on_40001`：错误码 40001 触发自动刷新并重试 | integration | done | `V1_RUST_LOCAL` |
| OR02 | authorizer access_token 刷新链 | refresh_token 换新链与过期语义 | `authorizer_access_token_refresh_chain`：force refresh 走 `get_authorizer_access_token` 链路 | integration | done | `V1_RUST_LOCAL` |
| OR03 | verify_ticket 存储链 | 回调 verify_ticket 必须写入 config（后续 component_token 刷新依赖） | `route_verify_ticket_stores_ticket`：route 后 config 可读回 ticket | integration | done | `V1_RUST_LOCAL` |
| OR04 | 回调 route 分发 | authorized/updateauthorized 授权码换 auth、notify_third_fasteregister、未知类型返回空 | `route_authorized_dispatch_exchanges_code`/`route_notify_third_fasteregister_dispatches`/`route_unknown_info_type_returns_empty` | integration | done | `V1_RUST_LOCAL` |
| OR05 | 加密回调端到端 | 加密 XML 推送（Encrypt 字段）解密→分发 | `route_encrypted_verify_ticket_end_to_end`：AES 加密报文经 route 解密落库 | integration | done | `V1_RUST_LOCAL` |
| OR06 | 预授权码/授权 URL | component 换预授权码、URL 拼接语义 | `get_pre_auth_code_and_url_build` | integration | done | `V1_RUST_LOCAL` |
| OR07 | 模板列表/草稿缺失语义 | 响应缺字段不得静默吞错（Java Gson 缺失行为对齐） | `get_template_list_parses`/`get_template_draft_list_none_when_absent` | integration | done | `V1_RUST_LOCAL` |
| OR08 | 上传 multipart 字节通道 | 素材/媒体文件 multipart 线格式（非 JSON） | `upload_minishop_media_file_multipart` | integration | done | `V1_RUST_LOCAL` |
| OR09 | minishop 分类手动解析 | 异构 JSON 结构需手动 from_value | `get_minishop_categories_manual_parse` | integration | done | `V1_RUST_LOCAL` |
| OR10 | mp/ma 桥接 token 委托 | 桥接服务必须走 authorizer token 缓存（按 appid 分桶）而非组件 token | `bridge_access_token_reads_open_authorizer_cache`/`bridge_services_cached_per_appid_and_independent` | integration | done | `V1_RUST_LOCAL` |
| OR11 | ma jsCode2Session 委托组件链路 | 必须走 `/sns/component/jscode2session`（component_access_token 注入） | `bridge_ma_jscode2session_delegates_component` | integration | done | `V1_RUST_LOCAL` |
| OR12 | 桥接请求注入 authorizer token | mp/ma 桥接的 get/post 必须注入 authorizer access_token | `bridge_ma_post_injects_authorizer_token`/`bridge_mp_post_injects_authorizer_token` | integration | done | `V1_RUST_LOCAL` |
| OR13 | open account 创建/绑定/解绑 errcode 语义 | errcode 非零转 Err、未知 app_id 类型拒绝 | `create_open_account_mp_request_and_result`/`bind_open_account_mini_returns_true`/`unbind_open_account_returns_false_without_errcode`/`open_account_rejects_unknown_app_id_type` | integration | done | `V1_RUST_LOCAL` |
| OR14 | oauth2 component URL | 代公众号网页授权 URL 携带 component_appid | `oauth2_get_access_token_parses` | integration | done | `V1_RUST_LOCAL` |
| OR15 | XML 消息线格式（单元） | verify_ticket/fasteregister 加密 XML 解析（含 CDATA/命名差异） | `wx_open_xml_message.rs`：`parse_verify_ticket`/`parse_fasteregister` | unit | done | `V1_RUST_LOCAL` |
| OR16 | 授权方信息 golden | snake_case 键 + func_info 扁平化 + MiniProgramInfo 大驼峰 | `authorizer_info_parses_java_golden`（src 单元 + tests 集成双份） | unit+integration | done | `V1_RUST_LOCAL` |
| OR17 | query_auth/authorizer_list 回写 | 授权码换 token 必须回写 config（access+refresh） | `get_query_auth_writes_authorizer_tokens`/`get_authorizer_list_writes_refresh_tokens` | integration | done | `V1_RUST_LOCAL` |
| OR18 | minishop 响应 null 镜像 | 响应字段缺失返回 None 而非空对象（Java Gson 语义） | `minishop_null_returns_mirror` | integration | done | `V1_RUST_LOCAL` |

## 四、`VALUE_ADD`：增量价值测试

| ID | 来源 | 风险/可捕获缺陷 | 新测试 | 与已有测试差异 | 状态 | 证据 |
|---|---|---|---|---|---|---|
| OV01 | 夹具复用审计 | Java WxOpenAuthorizerInfoResultTest 仅 print 无断言，夹具宝贵 | `authorizer_info_parses_java_golden`（同夹具逐字段断言） | Java 无断言；Rust 断言 nick_name/principal_name/MiniProgramInfo 等字段 | done | `V1_RUST_LOCAL` |
| OV02 | 组件契约审计 | Java 组件测试 37 空桩，component 88 方法契约无任何源测试 | component 19 个 MockServer 测试（token 链/route 分发/模板/open account/上传/oauth2） | 新增（Java 空桩） | done | `V1_RUST_LOCAL` |
| OV03 | 回调语义审计 | route 分发端到端（换码/回写/未知类型）在 Java 无断言 | `route_*` 5 个测试 + `route_encrypted_verify_ticket_end_to_end` | 新增（Java 空桩） | done | `V1_RUST_LOCAL` |
| OV04 | 错误语义审计 | errcode 非零/未知类型拒绝/解绑无 errcode | `open_account_rejects_unknown_app_id_type`/`unbind_open_account_returns_false_without_errcode`/`component_token_auto_refresh_on_40001` | 新增（Java 空桩） | done | `V1_RUST_LOCAL` |
| OV05 | 桥接正确性审计 | 代 mp/ma 服务的 token 委托链/注入/按 appid 分桶缓存 | bridge 12 个测试（含 downcast 取下转断言） | 新增（Java 空桩/注释演示） | done | `V1_RUST_LOCAL` |
| OV06 | 线格式审计 | minishop 异构 JSON 手动解析、响应 null 镜像 | `get_minishop_categories_manual_parse`/`minishop_null_returns_mirror` | 新增（Java 无对应断言） | done | `V1_RUST_LOCAL` |

## 五、差分与规范化

| Case schema/exporter | Java 原始输出 | Rust 原始输出 | normalizer 版本/规则 | 结果 | artifact |
|---|---|---|---|---|---|
| 自动跨语言差分 harness | 未搭建 | — | — | — | — |
| 黄金向量固化：authorizer_info 授权方信息 JSON（Java 测试夹具同源）、verify_ticket/fasteregister 加密 XML | 同左 | 同左 | 字节精确/字段断言 | 一致 | `tests/wx_open_component_service_test.rs` + `src/bean/*` 单元 |

## 六、测试价值审计

> 47 个 open 测试逐一满足：直接调用生产路径、可观察断言、确定性、去重（每个测试独立 MockServer 端口）。

| Rust 测试 | Trace | Observable | Mutation-sensitive | Production path | Deterministic | Distinct | 处置 |
|---|---|---|---|---|---|---|---|
| 34/34（component 19、bridge 12、lib 单元 3） | 是 | 是 | 是 | 是 | 是 | 是 | `KEEP` |

## 七、验收结果

| 门禁 | 命令/环境 | 通过/失败/跳过 | 证据级别 | artifact | 未验证边界 |
|---|---|---|---|---|---|
| source disposition（13 个测试对象：0 镜像/适配 + 3 未镜像 + 10 不适用） | 上表 O01–O13 | 通过 | `V0_STATIC`–`V1_RUST_LOCAL` | `docs/migration/weixin-java-open/迁移测试对照表.md` | Java TestNG 测试未执行；3 个 bean 测试对象未镜像（O08–O10） |
| Rust obligations（18 项） | `cargo test -p wx-rust-open` | 通过（47/47） | `V1_RUST_LOCAL` | `tests/wx_open_component_service_test.rs`、`tests/wx_open_mp_ma_bridge_test.rs`、`tests/wx_open_ma_domain_test.rs`、`src/bean/*` 单元 | — |
| coverage | `cargo llvm-cov --package wx-rust-open --summary-only` | 通过（open 44.97% line，TOTAL 42.80%） | `V6_NONFUNCTIONAL`（信号） | `覆盖审计报告.md` | 覆盖率不构成等价证明 |
| 对象台账 | `scripts/audit_open_ledger.py` | 240/240 处置；**21 MISSING**（Ma*/Minishop 20 + PrivacyKeyEnum 1） | `V0_STATIC` | `对象级对照表.md` | 缺口未补齐前阻断全量交付 |
| real host/rollback | 真实微信 API | 跳过 | — | — | V5 集成批次 |

## 八、结论

- 源测试处置率：13/13 全部处置，但**如实注明 0 镜像**——Java 测试对象绝大多数为空桩/无断言
  （组件 37、fastma 52、ma 80 空桩；oauth2 冒烟；authorizer_info 仅 print），无可镜像断言；
  3 个 FastMa bean 测试（O08–O10）对象已实现但未镜像。
- Rust 义务 18/18 通过（token 双检锁/40001 自动续期/回调 route 分发/加密回调端到端/桥接委托链/
  上传通道/golden 线格式/错误语义）。
- VALUE_ADD 6 项（夹具复用断言、组件契约 19 测、route 分发、错误语义、桥接正确性、线格式镜像）。
- **对象缺口 21 个如实保留**（Ma*/Minishop 子域服务 20 + PrivacyKeyEnum 1），
  `scripts/audit_open_ledger.py` 可重放翻转；补齐前模块状态 ⚠️ 主体交付（非全量）。
- 模块状态：⚠️ 主体交付（546 workspace tests 全绿，open 34；21 对象 MISSING 阻断全量）。
