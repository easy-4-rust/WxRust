# weixin-java-cp 覆盖审计报告（Wave 5 交付后审计）

> 审计日期：2026-08-01
> Java 基线：`a49d6e1461752c06b752d2afd8aeeb7e6e78cefe`
> Rust 基线：`2026-08-01 working-tree（crates/wx-rust-cp，599 个 .rs 文件；cargo test --workspace 546/546 全绿；cp 118）`
> 测试命令：`cargo test --workspace --all-features`（**546/546**，cp 118：集成 55 + src 内嵌单元 63）
> 覆盖命令：`cargo llvm-cov --package wx-rust-cp --summary-only`
> 配套文档：`迁移测试对照表.md`、`对象级对照表.md`（594 对象处置 546：517 IMPLEMENTED + 20 PLATFORM_NA + 9 DEPENDENCY_REUSED；**48 MISSING 阻断**）

## 一、SOURCE_PARITY 处置覆盖

| 维度 | 数值 |
|---|---|
| Java 测试对象（模块） | 88 |
| 已处置 | 88（41 镜像/适配 + 33 未镜像 + 14 不适用） |
| Java 测试执行 | 未运行（TestNG；Maven surefire 默认不执行） |
| Rust 测试 | 118（sub_domain_cp_core 17 / sub_domain_cp_facade 6 / wx_cp_extra_test 18 / wx_cp_service_impl_test 14 + src 内嵌单元 63），全部通过 |

**处置依据**：`NOT_MIRRORED` 33 项中 9 项对应 tp 批次未实现（台账 MISSING 34 中的 tp 面）、
1 项 corpgroup 未实现、20 项 bean/out 消息线格式无镜像、1 项消息 XML 加解密往返无镜像、
1 项重试语义已实现无镜像、1 项 tp 消息路由未实现；`NOT_APPLICABLE` 14 项对应 Guice/Redis/
native SDK 配置/宿主示例与 Apache HTTP 后端。

## 二、行为证据层级（V0–V6）

| 层级 | 覆盖对象 | 证据位置 |
|---|---|---|
| V0_STATIC | 结构/URL 常量/线格式声明 | 599 个 .rs 文件结构；`enums/url_*.rs` 23 个常量文件 |
| V1_RUST_LOCAL | 118 个测试（MockServer 集成 4 组 + impl 内嵌单元 63） | `tests/sub_domain_cp_core.rs`（17）/`sub_domain_cp_facade.rs`（6）/`wx_cp_extra_test.rs`（18）/`wx_cp_service_impl_test.rs`（14）+ 24 个 src 文件内嵌 `#[cfg(test)]` |
| V2_GOLDEN | 跨语言黄金（Java/官方派生） | jsapi 签名 SHA1、user/tag/depart/chat/kf 线格式、会话存档 RSA 解密（PKCS1/PKCS8）、路由同步分发语义 |
| V3–V6 | 未做（proptest/宿主/回滚） | — |

## 三、Rust 义务完成度

14 项义务测试全部通过（见对照表 CR01–CR14），关键项：
- **缓存/并发**：token 双检锁（并发调用仅 1 次 HTTP）、contact/msg_audit 专用 token 通道
- **签名/黄金**：jsapi/agent jsapi 签名 SHA1 黄金重算、qr 连接 URL 保留未编码字符
- **消息路由**：同步分发（逐例镜像 Java）、异步执行、拦截器、重复消息去重、正则与内容匹配
- **会话存档**：RSA+AES 两段式解密（PKCS1/PKCS8 黄金）、6 个 native SDK 拉取方法显式返回 -99 未实现（PLATFORM_NA 边界固化）
- **装配**：31 个 getter 单例安装 + 门面引擎透传；post_without_token 不注入 token

## 四、VALUE_ADD

7 项（CV01–CV07）：并发缓存计数断言、路由去重、私钥解密拒绝路径（PKCS 模式缺失/损坏输入）、
native SDK 方法未实现边界、user/tag/depart/chat/kf 线格式黄金、31 getter 装配不变量、
46003 无菜单错误语义。

## 五、覆盖率与可比 scope

| 指标 | 值 | 说明 |
|---|---|---|
| cp line | **42.27%**（10814 行） | 交付后线覆盖 |
| cp fn | **26.21%**（2728） | — |
| cp region | **43.67%**（15963） | — |
| workspace line | **42.80%**（58699 行） | 同会话全 workspace 汇总 |
| workspace fn | **28.55%** | — |
| workspace region | **40.67%** | — |
| cp 报告文件数 | 361 / 599 个 .rs | 其余文件为纯声明/派生文件，无独立可执行计数 |
| cp 0% 文件数 | 273 | 明细见下 |
| cp 0<line<50 文件数 | 25 | — |
| cp line≥90 文件数 | 9 | `wx_cp_msg_audit_service_impl.rs`（92.74%）、`wx_cp_message_router_rule.rs`（96.08%）、`wx_cp_corp_group_service_impl.rs`/`wx_cp_hr_service_impl.rs`/`wx_cp_agent_jsapi_signature.rs`/`wx_cp_host_config.rs`/`wx_cp_xml_out_text_message.rs`/`outxmlbuilder` 2 个 |

**关键文件覆盖**：`wx_cp_msg_audit_service_impl.rs` 92.74%（会话存档解密/HTTP 通道/native SDK 边界）、
`wx_cp_message_router.rs` 89.95% + `wx_cp_message_router_rule.rs` 96.08%（路由族）、
`wx_cp_service_impl.rs` 83.78%（门面装配/执行引擎）、`util/crypto/wx_cp_crypt_utils.rs` 71.76%（会话存档 RSA）、
`base_wx_cp_service_impl.rs` 61.47%（重试执行引擎）、`api/wx_cp_service.rs` 39.83%（门面 trait 默认实现）、
`config/wx_cp_config_storage.rs` 17.95%（配置 trait）。

**覆盖率解读（为什么 line% 偏低）**：

1. **bean 密度高、结构体自身贡献低（结构性主因）**：cp 594 行台账中约 420 个是 bean。
   serde 派生结构体声明行计入可执行行分母，但 0% 文件 273 个中的绝大多数是 bean 声明文件
   （bean/oa 82、bean/external 52、bean/school 30、bean/license 18、bean/kf 8、bean/living 6、
   bean/intelligentrobot 6、bean/msgaudit 3、bean/corpgroup 3 …）——服务测试经"请求/响应解析"
   触达的是 from_json/Deserialize 宏展开，结构体自身声明行与未走到的 `#[serde]` 分支不计数。
2. **tp/corpgroup 域 0% 与台账 MISSING 一一对应**：bean/license 18 个 0%（tp 授权 bean，
   tp 批次未实现）；bean/wx_tp_* 与 wx_cp_tp_xml_package 等 tp bean 0%；corpgroup bean 3 个 0%。
3. **out 消息与 builder 面零触达**：message/outxmlbuilder 7 + message/messagebuilder 9 个 0%——
   Java 侧 6 个 XmlOut 消息测试未镜像（CP13），builder 无测试（对应义务面缺口）。
4. **服务层覆盖集中**：31 个子服务 impl 以「集成测试（17 个 core 用例）+ impl 内嵌单元（63 个）」
   双通道覆盖，核心服务文件 60%–96% line；每服务仅 1–3 个代表性用例，参数校验/错误分支未全覆盖。

## 六、改进建议

| 建议 | 类型 | 收益 | 工作量 |
|---|---|---|---|
| 补齐 tp 批次 34 对象（14 接口 + 15 实现 + 5 消息路由）并各带镜像测试 | 对象+行为 | 解除 48 MISSING 中的 34；license/tp bean 与 WxCpTpMessageRouterTest 处置翻转 | 高（整批 tp） |
| corpgroup 4 对象（CgService/LinkedCorpService + impl） | 对象+行为 | 解除 corpgroup 缺口；bean/corpgroup 0% 翻转 | 中 |
| out 消息 6 个 Java 测试镜像（XmlOut Text/News/TaskCard/Image/Video/Voice） | 行为 | outxmlbuilder 0% 翻转；对齐 Java WxCpXmlOut*Test | 低（按既有模板） |
| WxCpCryptUtilTest 镜像（消息 XML 加解密往返） | 行为 | crypt_utils XML 面补镜像 | 低 |
| WxCpBusyRetryTest 镜像（指数退避重试断言） | 行为 | execute_with_retry 路径补断言 | 低 |
| bean 请求侧 Option 字段组合的 proptest 差分 | 非功能 | 覆盖 serde 省略/默认分支 | 中 |
| Java TestNG 测试执行（JDK 8） | 工具链 | 源测试真值基线 | 低（配置） |
| V5 真实企微 API 宿主与 Redis 外部存储 | 宿主 | 端到端与 PLATFORM_NA 项收口 | 高 |

## 七、结论

- 源测试 88/88 处置；义务 14/14；VALUE_ADD 7 项；546 workspace 测试全绿（cp 118）。
- 覆盖率（cp 42.27% line）作为**信号**：服务路径层（核心 impl 60%–96%）与路由族（90%+）覆盖集中，
  bean 声明层与 tp/corpgroup/out 消息域拉低分母；未覆盖热点已精确到对象级并与台账 MISSING 对应。
- **594 对象处置 546，48 MISSING（tp 34 + corpgroup 4 + config 4 + OaMail 2 + OnTp + WxCpTpConsts + WxCpTpCryptUtil + AttachmentBuilder）如实列出**——
  模块状态：⚠️ 部分交付（核心 API + bean + 消息路由 + 会话存档 RSA 面交付，tp/corpgroup 面阻断全量结论）。
