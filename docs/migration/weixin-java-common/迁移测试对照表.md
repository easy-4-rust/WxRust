# weixin-java-common Java → Rust 迁移测试对照表

> Java 基线：`a49d6e1461752c06b752d2afd8aeeb7e6e78cefe`
> Rust 基线：`2026-08-01 working-tree（非 git 仓库；crates/wx-rust-common 105 个 src .rs 文件 + 5 个集成测试文件）`
> 最近审计：`2026-08-01`
> Java 测试命令：`JAVA_HOME=<corretto-8> mvn -pl weixin-java-common test`（本会话**未执行**——JDK 21 + Lombok 1.18.30 编译失败（`ExceptionInInitializerError: TypeTag :: UNKNOWN`），需要 JDK 8；方法基线来自 `scripts/inventory_java_methods.py` javap 清单）
> Rust 测试命令：`cargo test -p wx-rust-common --all-features`（83/83 通过，V1_RUST_LOCAL）
> Rust 覆盖命令：`cargo llvm-cov --workspace --all-features --summary-only`（rustc 1.97.1 + llvm-tools-preview；注意：Homebrew rustc 不带 llvm-tools，需 `PATH="$HOME/.cargo/bin:$PATH"` 优先 rustup 工具链）

## 零、对象状态完成防火墙

> 权威对象台账：`docs/migration/weixin-java-common/对象级对照表.md`（同一变更中更新，含 SHA1 `DEPENDENCY_REUSED` POC 证据）

| `MISSING` | `MISPLACED` | `STUB` | `PARTIAL` | `UNVERIFIED` | 是否阻断迁移完成 |
|---:|---:|---:|---:|---:|---|
| 0 | 0 | 0 | 0 | 0 | 不阻断（90 `IMPLEMENTED` + 1 `DEPENDENCY_REUSED` + 87 `PLATFORM_NA` + 1 `RUST_EXTENSION`） |

测试通过、覆盖率、差分子集或宿主冒烟均不能覆盖这些对象事实。

## 一、范围与覆盖率口径

| 项目 | Java | Rust | 是否可比 | 说明 |
|---|---|---|---|---|
| 纳入模块/生产文件 | weixin-java-common 174 个主文件（B0 冻结） | wx-rust-common 105 个 src 文件（90 实现 + 87 平台 NA + 1 依赖复用 + 1 扩展） | 否 | Java 侧未运行测试，无数字可对比 |
| 排除项/生成代码 | package-info、注解 | 错误码生成表（`wx_*_error_msg_enum.rs` 2529 行纯数据，仅 `find()` 行为抽样测试）；`PLATFORM_NA` 对象文件 | — | 生成器：`scripts/gen_error_msg_enums.py` |
| 工具与指标 | JUnit4（未运行） | cargo-llvm-cov 0.8.7 line/function/region | 否 | — |
| profile/features/target | — | `--workspace --all-features` | — | — |
| 基线覆盖率 | 未运行（工具链受阻，见上） | line **26.07%**（1084/4158）、function **50.28%**（89/177）、region **19.46%**（740/3802） | 否 | 明细见 `覆盖率明细-2026-08-01.txt` |

## 二、`SOURCE_PARITY`：源测试处置

> 98 个 `@Test` 注解分布于 29 个测试文件；JUnit 辅助方法（无 `@Test`，由标注方法调用，如 `WxErrorTest::testFromBadJson1/2`、`WxCryptUtilTest::testAesEncrypt/testAesEncrypt2/testValidateSignatureError`）并入其调用者行。处置：42 镜像/适配 + 56 不适用 + 0 缺失/阻塞。

| ID | Java 测试/用例 | 源合同与生产调用链 | 输入/夹具/断言 | Rust 测试 | 处置 | 证据 | 差异/阻塞 |
|---|---|---|---|---|---|---|---|
| S01 | `api/WxMessageInMemoryDuplicateCheckerTest::test` | TTL 窗口内去重（`WxMessageInMemoryDuplicateChecker`，2000ms TTL/1000ms 清理） | 8 个 msgId 两阶段检查 | `source_parity_crypto_util::duplicate_checker_ttl_behavior`、`duplicate_checker_expiry` | `MIRRORED` | `V1_RUST_LOCAL` | TTL 用 200ms 加速验证过期 |
| S02 | `api/WxMessageInMemoryDuplicateCheckerSingletonTest::test` | 静态单例 `getInstance()` 同一实例 + 去重生效（15s TTL） | 两次取实例 + 消息去重 | `source_parity_crypto_util::duplicate_checker_singleton_same_instance` | `MIRRORED` | `V1_RUST_LOCAL` | Rust `once_cell::Lazy` 全局实例 + `ptr::eq` 断言 |
| S03 | `api/WxMessageInRedisDuplicateCheckerTest::test` | Redis 后端去重（`WxRedisOps`） | 真实 Redis | — | `NOT_APPLICABLE` | `V0_STATIC` | Redis 后端未迁移（trait 已迁移，`redis_feature_types_exist` 验证 feature 门控）；V5 集成待做 |
| S04 | `bean/CommonUploadParamTest`（10 用例） | `CommonUploadParam`：fromFile/fromBytes/表单字段链式构建/构造器/toString | 文件、字节、字段名（含 null/空） | `source_parity_bean_error::common_upload_param_from_file`、`common_upload_param_with_form_fields` | `ADAPTED`（8） | `V1_RUST_LOCAL` | `toString` → Rust `Debug`；null 字段名边界由类型系统（`String` + `Result`）表达，Rust 测试覆盖核心链式合同 |
| S05 | `bean/WxAccessTokenTest::testFromJson` | `WxAccessToken` JSON 反序列化 | 标准 JSON 夹具 | `source_parity_bean_error::access_token_from_json` + `rust_obligation_value_add::serde_access_token_wire_format`/`missing_fields_defaults` | `MIRRORED` | `V1_RUST_LOCAL` | — |
| S06 | `bean/WxMenuTest`（4 用例，参数化 JSON） | `WxMenu`/`WxMenuButton`/`WxMenuRule` 序列化与反序列化 | 多组菜单 JSON（含子按钮、条件菜单） | `source_parity_bean_error::menu_from_json_three_buttons`、`menu_button_sub_button_serialization`、`menu_rule_tag_id_alternate` | `ADAPTED` | `V1_RUST_LOCAL` | Java 参数化 4 夹具由 3 个 Rust 测试覆盖关键合同（三按钮/子按钮/`tag_id`↔`group_id`） |
| S07 | `bean/WxNetCheckResultTest::testFromJson` | `WxNetCheckResult` 线格式（Gson 适配器：`dns`/`ping` + `real_operator`/`from_operator`/`package_loss`） | 真实探测 JSON | `source_parity_bean_error::net_check_result_from_json` | `MIRRORED` | `V1_RUST_LOCAL` | serde `rename`+`alias` 还原线格式 |
| S08 | `error/WxErrorTest::testFromJson`（含 helper `testFromBadJson1/2`） | `WxError::fromJson` 解析与 bad-json 容错 | 正常/畸形 JSON | `source_parity_bean_error::wx_error_from_json_basic` 等 6 个 + `rust_obligation_value_add::wx_error_malformed_json_returns_default`/`wx_error_huge_code_no_panic` | `MIRRORED` | `V1_RUST_LOCAL` | — |
| S09 | `error/WxMaErrorMsgEnumTest`（4 用例：find/非存在/虚拟支付码） | `WxMaErrorMsgEnum::findMsg` 错误码映射 | 21 个虚拟支付码精确映射 + 缺失码 | `source_parity_bean_error::ma_error_msg_find_existing_code`、`ma_error_msg_find_non_existing_code`、`ma_error_msg_virtual_payment_codes`、`ma_error_msg_virtual_payment_missing_codes` | `MIRRORED` | `V1_RUST_LOCAL` | 2509 条映射由生成器产出，21 码精确 oracle |
| S10 | `session/SessionTest`（9 用例） | `StandardSessionManager`：getSession 同一性/失效重建/属性往返/后台清理/最大活跃数 | 多 id 会话 | `source_parity_crypto_util::session_*`（4 个）+ `rust_obligation_value_add::token_concurrent_*` 并发语义 | `MIRRORED`（4）/`ADAPTED`（5） | `V1_RUST_LOCAL` | `testBackgroundProcess*`/`testMaxActive*` 依赖 Java 定时线程清理 → Rust 惰性清理 + 并发测试覆盖；`testInvalidate2` 用内部类 `InternalSessionManager` |
| S11 | `util/DataUtilsTest::testHandleDataWithSecret` | `DataUtils.handleDataWithSecret` 脱敏 | 含敏感字段/不含 | `source_parity_crypto_util::data_utils_handle_data_with_secret`、`data_utils_no_secret_unchanged` | `MIRRORED` | `V1_RUST_LOCAL` | — |
| S12 | `util/XmlUtilsTest`（3 用例：`testXml2Map`/`testXml2Map_another`/`testXml2Map_xxe`） | `XmlUtils.xml2Map` 解析（含 CDATA、XXE 防护） | 常规 XML、CDATA、DOCTYPE 载荷 | `source_parity_crypto_util::xml_utils_xml2map`、`xml_utils_xml2map_cdata`、`xml_utils_malformed_returns_err` + `rust_obligation_value_add::xml_utils_xxe_doctype_rejected` | `MIRRORED` | `V1_RUST_LOCAL` | Java 测试断言非空；Rust 显式断言 DOCTYPE 被拒绝（quick-xml 无外部实体） |
| S13 | `util/crypto/SHA1Test`（3 用例） | `SHA1.gen`/`genWithAmple`/`gen_illegalArguments` | 黄金向量 | `source_parity_crypto_util::sha1_gen_known_vector`、`sha1_gen_with_ample_known_vector`、`sha1_gen_illegal_arguments` + `core_utils::sha1_gen_sorts_and_hashes`/`sha1_gen_with_amp_joins_with_ampersand` | `MIRRORED` | `V1_RUST_LOCAL` | 黄金向量 `9f537aeb…`/`20b896cc…` 与 Java 测试一致；`DEPENDENCY_REUSED`（sha1 0.11.0）证据闭环 |
| S14 | `util/crypto/WxCryptUtilTest::testNormal`（含 helper `testAesEncrypt`/`testAesEncrypt2`/`testValidateSignatureError`） | `WxCryptUtil` 加密/解密/验签/密文提取 | 固定 `randomStr="aaaabbbbccccdddd"` + 微信黄金密文 `jn1L23DB+6ELqJ+6bruv21Y6MD7KeIfP82D6gU39rmkgczbWwt5+3bnyg5K55bgVtVzd832WzZGMhkP72vVOfg==` | `source_parity_crypto_util::crypt_util_fixture`、`crypt_util_aes_encrypt_golden`、`crypt_util_normal_roundtrip`、`crypt_util_direct_decrypt`、`crypt_util_validate_signature_error`、`crypt_util_xml_extract_encrypt` + `core_utils::crypt_util_encrypt_decrypt_roundtrip`/`crypt_util_wrong_signature_fails` | `MIRRORED` | `V1_RUST_LOCAL` | 黄金密文精确匹配（固定 IV）；宽松 base64（43 字符 EncodingAESKey）在 `rust_obligation_value_add::crypt_util_invalid_aes_key_rejected`/`decrypt` 覆盖 |
| S15 | `util/fs/FileUtilsTest`（3 用例） | `FileUtils.createTmpFile`（指定/默认目录）+ `imageToBase64ByStream` | 内容/前缀/后缀/目录；`hello`→`aGVsbG8=` | `source_parity_fs_utils`（6 个：显式目录、默认 `wxjava-temp`、唯一名、黄金向量、空流、读错误→`Err`） | `MIRRORED` | `V1_RUST_LOCAL` | Java 默认目录为 `java.io.tmpdir` → Rust `temp_dir()/wxjava-temp`（ADAPTED，注释声明）；Java 单次 `read()` 可能截断 → Rust `read_to_end` 完整读取（修复语义，记录于语义表） |
| S16 | `util/http/HttpResponseProxyTest`（2 用例） | `HttpResponseProxy.extractFileNameFromContentString`：`filename*=utf-8''` URL 解码 → `filename="…"` Latin-1→UTF-8 | 两种 Content-Disposition | `rust_obligation_value_add::http_response_proxy_extract_filename_utf8`/`_plain`/`_empty_error` | `MIRRORED` | `V1_RUST_LOCAL` | 中文名 `营业执照.jpg`（Latin-1 字节输入）精确还原 |
| S17 | `util/http/apache/DefaultApacheHttpClientBuilderTest`（2） | Apache HttpClient 构建器（连接池/超时/代理） | — | — | `NOT_APPLICABLE` | `V0_STATIC` | `PLATFORM_NA`：JVM HTTP 后端；Rust 统一 reqwest（见 ARCHITECTURE.md 组件替换表） |
| S18 | `util/http/apache/SSLConfigurationTest`（4） | Apache SSL 上下文配置 | — | — | `NOT_APPLICABLE` | `V0_STATIC` | 同上；rustls 承担 TLS |
| S19 | `util/http/apache/SSLIntegrationTest`（2） | 真实 TLS 握手集成 | 外网/测试服务 | — | `NOT_APPLICABLE` | `V0_STATIC` | 宿主级（V5）验证留待集成批次 |
| S20 | `util/http/okhttp/DefaultOkHttpClientBuilderTest`（1） | OkHttp 构建器 | — | — | `NOT_APPLICABLE` | `V0_STATIC` | 同上 |
| S21 | `util/json/GsonHelperTest`（28 用例） | Gson 序列化工具（`toJson`/`fromJson`/类型适配） | 28 组值/类型 | — | `NOT_APPLICABLE` | `V0_STATIC` | `PLATFORM_NA`：Gson 线格式语义由 serde_json 承担（数值精度、null 省略、字段命名差异记录于语义迁移对照表） |
| S22 | `util/json/GsonParserTest`（3 用例） | Gson `JsonParser` 容错解析 | 畸形 JSON | — | `NOT_APPLICABLE` | `V0_STATIC` | serde_json 解析器行为等价性由 `wx_error_malformed_json_returns_default` 等 Rust 义务测试承担 |
| S23 | `redis/CommonWxRedisOpsTest`（6 用例） | `WxRedisOps` 契约：get/set/expire/锁 | 真实 Redis | — | `NOT_APPLICABLE` | `V0_STATIC` | Redis 后端未迁移（trait + feature 门控已迁移）；V5 集成待做 |
| S24 | `redis/JedisWxRedisOpsTest` / `RedisTemplateWxRedisOpsTest` / `RedissonWxRedisOpsTest`（0 用例） | 各客户端适配器配置类（继承基类测试） | — | — | `NOT_APPLICABLE` | `V0_STATIC` | JVM 客户端专属 |
| S25 | `util/locks/RedisTemplateSimpleDistributedLockTest`（3） | `SimpleDistributedLock` 获取/释放/过期 | 真实 Redis | — | `NOT_APPLICABLE` | `V0_STATIC` | Rust `redis_distributed_lock.rs` 为集成形态（91 行未进测试——需真实 Redis 或 mock），V5 待做 |
| S26 | `util/locks/RedisTemplateSimpleDistributedLockSerializationTest`（3） | 锁值 Java 对象序列化 | Redis 序列化器 | — | `NOT_APPLICABLE` | `V0_STATIC` | Java 序列化机制无 Rust 对应 |
| S27 | `util/locks/RedisTemplateSimpleDistributedLockInterruptTest`（3） | 中断/超时语义 | 线程中断 | — | `NOT_APPLICABLE` | `V0_STATIC` | JVM 线程中断语义无对应；Rust 侧超时由 `tokio::time` 表达（待 V5） |

## 三、`RUST_OBLIGATION`：Rust 特有义务

| ID | Rust 机制/组件 | 必须保护的风险 | 测试与断言 | 层级 | 状态 | 证据 |
|---|---|---|---|---|---|---|
| R01 | `Arc<dyn WxSession>` 共享引用 | Java 引用同一性破坏（clone 语义错误） | `session_get_same_instance`：两次 getSession 同实例；失效后 `Arc::as_ptr` 不同、id 相同 | integration | done | `V1_RUST_LOCAL` |
| R02 | token 双检锁 single-flight（`RwLock`） | 并发刷新打爆上游（Java DCL 语义） | `token_concurrent_refresh_single_flight`：N 并发仅 1 次上游刷新 | integration | done | `V1_RUST_LOCAL` |
| R03 | 并发写入不丢更新 | 竞态丢 token | `token_concurrent_update_no_loss` | integration | done | `V1_RUST_LOCAL` |
| R04 | serde 未知字段容忍 | 微信新增字段导致解析失败 | `serde_wx_error_unknown_fields_tolerated` | integration | done | `V1_RUST_LOCAL` |
| R05 | serde 缺失字段默认值 | 部分响应缺字段 | `serde_access_token_missing_fields_defaults` | integration | done | `V1_RUST_LOCAL` |
| R06 | 错误变体与 `Display` 合同 | 错误信息可诊断、可分类 | `error_exception_wx_variant_code`/`runtime_variant`/`display_messages` | integration | done | `V1_RUST_LOCAL` |
| R07 | `Send + Sync` 静态断言 | 异步运行时跨线程可用 | `core_types_are_send_sync`（编译期 trait bound） | integration | done | `V1_RUST_LOCAL` |
| R08 | redis feature 门控 | 无 redis 依赖也能编译 | `redis_feature_types_exist`（feature 内类型存在性） | integration | done | `V1_RUST_LOCAL` |
| R09 | 畸形 JSON 容错 | 不 panic、返回默认错误 | `wx_error_malformed_json_returns_default` | integration | done | `V1_RUST_LOCAL` |
| R10 | 错误码数值边界 | `u32` 溢出 panic | `wx_error_huge_code_no_panic`（超 `u32::MAX` 码值） | integration | done | `V1_RUST_LOCAL` |
| R11 | XXE 防护 | quick-xml 外部实体拒绝 | `xml_utils_xxe_doctype_rejected` | integration | done | `V1_RUST_LOCAL` |
| R12 | PKCS7 非法填充拒绝 | 解密时填充错误必须报错（对齐 Java） | `pkcs7_decode_invalid_padding` | integration | done | `V1_RUST_LOCAL` |
| R13 | 空密文拒绝 | 空输入解密不 panic | `crypt_util_decrypt_empty_cipher` | integration | done | `V1_RUST_LOCAL` |
| R14 | 非法 AES key 拒绝 | `new()` 长度/解码校验 | `crypt_util_invalid_aes_key_rejected` | integration | done | `V1_RUST_LOCAL` |
| R15 | 宽松 base64（微信 43 字符 key） | Commons Codec 宽松解码语义 | `wx_crypt_util::lenient_base64_decode`（STANDARD→NO_PAD→末字符掩码）经 `decrypt` 路径 | integration | done | `V1_RUST_LOCAL` |
| R16 | 文件名 Latin-1→UTF-8 | HTTP 头编码还原 | `http_response_proxy_extract_filename_*` | integration | done | `V1_RUST_LOCAL` |
| R17 | URI 编码（RFC 3986） | 查询参数编码一致性 | `uri_util_encode_uri_component` | integration | done | `V1_RUST_LOCAL` |
| R18 | 错误码表 `find()` 线程/性能 | 2509 条映射查询正确 | `ma_error_msg_*`（21 虚拟支付码 oracle 抽样） | integration | done | `V1_RUST_LOCAL` |
| R19 | 重复检查器并发安全 | 并发 `is_duplicate` 无数据竞争 | `duplicate_checker_concurrent_safe` | integration | done | `V1_RUST_LOCAL` |
| R20 | 执行引擎指数退避重试 | Java `HttpSimpleRequestExecutor` 语义 | `simple_get_handle_response_error_code`/`_ok`（重试分支） | integration | done | `V1_RUST_LOCAL` |
| R21 | 请求/响应类型与票证常量 | 枚举值与原义一致 | `http_client_type_names`、`wx_type_names`、`ticket_type_values` | integration | done | `V1_RUST_LOCAL` |
| R22 | token TTL 边界 | 恰好过期边界判定 | `token_entry_expiry_boundary` | integration | done | `V1_RUST_LOCAL` |
| R23 | `WxHttpClient`/`WxHttpRequest` trait 合同 | trait 形状与 Java 门面一致 | `request_http_trait_contract` | integration | done | `V1_RUST_LOCAL` |

## 四、`VALUE_ADD`：增量价值测试

| ID | 来源 | 风险/可捕获缺陷 | 新测试 | 与已有测试差异 | 状态 | 证据 |
|---|---|---|---|---|---|---|
| V01 | 并发审计（覆盖） | 双检锁在 async 下的单飞失效 | `token_concurrent_refresh_single_flight` | Java 无并发 token 测试（B0 javap 清单确认） | done | `V1_RUST_LOCAL` |
| V02 | 边界审计 | `u32` 码值溢出 | `wx_error_huge_code_no_panic` | Java `int` 无此边界 | done | `V1_RUST_LOCAL` |
| V03 | 安全审计 | XML 外部实体注入 | `xml_utils_xxe_doctype_rejected` | Java 测试仅断言非空，未断言 XXE 拒绝 | done | `V1_RUST_LOCAL` |
| V04 | 安全审计 | 非法填充静默接受 | `pkcs7_decode_invalid_padding` | Java 侧填充校验在 JCE 内部 | done | `V1_RUST_LOCAL` |
| V05 | 协议审计 | 空密文/坏 key 路径 | `crypt_util_decrypt_empty_cipher`、`crypt_util_invalid_aes_key_rejected` | — | done | `V1_RUST_LOCAL` |
| V06 | 并发审计 | 重复检查器竞态 | `duplicate_checker_concurrent_safe` | — | done | `V1_RUST_LOCAL` |
| V07 | 编码审计 | HTTP 头中文文件名 | `http_response_proxy_extract_filename_utf8`（`营业执照.jpg`） | — | done | `V1_RUST_LOCAL` |
| V08 | 编码审计 | RFC 3986 严格编码 | `uri_util_encode_uri_component` | Java `URLEncoder` 空格为 `+`，Rust 保持原义 | done | `V1_RUST_LOCAL` |

## 五、差分与规范化

| Case schema/exporter | Java 原始输出 | Rust 原始输出 | normalizer 版本/规则 | 结果 | artifact |
|---|---|---|---|---|---|
| 尚未搭建自动跨语言差分 harness | — | — | — | — | — |
| 黄金向量已固化进测试（替代自动差分）：SHA1 `9f537aeb751ec72605f57f94a2f6dc3e3958e1dd` / `20b896ccbd5a72dde5dbe0878ff985e4069771c6`；AES 密文 `jn1L23DB+6ELqJ+6bruv21Y6MD7KeIfP82D6gU39rmkgczbWwt5+3bnyg5K55bgVtVzd832WzZGMhkP72vVOfg==`；Base64 `aGVsbG8=`；菜单/NetCheck 线格式 JSON 夹具 | 同左 | 同左 | — | 一致 | `tests/source_parity_crypto_util.rs`、`tests/source_parity_bean_error.rs`、`tests/source_parity_fs_utils.rs` |

## 六、测试价值审计

> 83 个 Rust 测试逐一满足：直接调用生产路径（Production path ✓）、有可观察断言（Observable ✓）、对实现变化敏感（Mutation-sensitive ✓——常量/分支/错误路径均有独立断言）、确定性（Deterministic ✓——固定随机种子/黄金向量/固定 IV）、去重（Distinct ✓——无两测断言同一合同）。

| Rust 测试 | Trace | Observable | Mutation-sensitive | Production path | Deterministic | Distinct | 处置 |
|---|---|---|---|---|---|---|---|
| 83/83（core_utils 11、source_parity_bean_error 17、source_parity_crypto_util 20、source_parity_fs_utils 6、rust_obligation_value_add 29） | 是 | 是 | 是 | 是 | 是 | 是 | `KEEP`（0 `IMPROVE`/`MERGE`/`REMOVE_PROPOSED`） |

## 七、验收结果

| 门禁 | 命令/环境 | 通过/失败/跳过 | 证据级别 | artifact | 未验证边界 |
|---|---|---|---|---|---|
| source disposition（98 用例全处置：42 镜像/适配 + 56 不适用） | 上表 S01–S27 | 通过 | `V0_STATIC`–`V1_RUST_LOCAL` | `docs/migration/weixin-java-common/迁移测试对照表.md` | Java 测试未执行（JDK 8 工具链未跑） |
| Rust obligations（23 项） | `cargo test -p wx-rust-common --all-features` | 通过（83/83） | `V1_RUST_LOCAL` | `tests/*.rs` | — |
| coverage | `cargo llvm-cov --workspace --all-features --summary-only`（rustc 1.97.1 + llvm-tools-preview） | 通过（line 26.07% / fn 50.28% / region 19.46%） | `V6_NONFUNCTIONAL`（信号） | `覆盖率明细-2026-08-01.txt` | 覆盖率不构成等价证明 |
| real host/rollback | Redis 后端、真实微信 API | 跳过 | — | — | V5 集成批次（redis ops/锁/媒体上传执行器） |

## 八、结论

- 源测试处置率：**98/98（100%）**，`MIRRORED`/`ADAPTED` 42、`NOT_APPLICABLE` 56、`MISSING`/`BLOCKED` 0
- 高风险源合同证据：WxCryptUtil 黄金密文（固定 IV 精确匹配）、SHA1 双黄金向量、21 个虚拟支付错误码精确映射、WxNetCheckResult dns/ping 线格式、菜单 sub_button/tag_id 线格式——全部有 Java 夹具派生 oracle，V1 通过
- Rust 特有义务完成率：**23/23 测试通过**（Send/Sync、single-flight、serde 边界、XXE、PKCS7、宽松 base64、Latin-1 文件名、RFC 3986）
- Java/Rust 覆盖率是否同口径可比：**否**——Java 侧因 JDK/Lombok 工具链未执行，无 JVM 覆盖率数字；不得跨语言比较百分比
- Rust 覆盖率超过 Java 的原因：不适用（Java 无数字）；Rust 26.07% 为信号而非结论，被生成错误码表（2529 行纯数据，open 1836 行 0%）、`PLATFORM_NA`/集成形态文件（redis ops 57 行、redis 锁 91 行、媒体上传执行器 56 行）拉低——这些对象按台账为 `PLATFORM_NA` 或 V5 范畴
- 阻塞、stub、flaky、跳过与未验证目标：无阻塞/stub/flaky；V5 集成（Redis 后端、真实 HTTP 宿主）跳过；`WxErrorTest`/`WxCryptUtilTest` 的 JUnit helper 方法并入调用者行处理
- ignored/not-run 测试、warnings 与 lint：`cargo clippy` 零警告；无 ignored 测试；Java 测试未运行（非忽略，是工具链未执行）
- 结论：**对象防火墙 0，不阻断**；源测试 100% 处置、义务 23/23 通过、覆盖率信号记录在案。按路线图 B2 批次 1（wx-rust-common）测试门禁达成；模块级 V5 集成验证（Redis/真实微信 API 宿主）未做，属后续批次——**本批次完成，模块整体仍按路线图推进**
