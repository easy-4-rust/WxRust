# weixin-java-common Java → Rust 功能语义迁移对照表

> 生成日期：2026-08-01
> Java 基线：`a49d6e1461752c06b752d2afd8aeeb7e6e78cefe`
> Rust 基线：`2026-08-01 working-tree（crates/wx-rust-common 105 src 文件 + 5 集成测试文件）`
> 最近按 Rust 基线审计：2026-08-01（B2 批次 1 完成：83/83 测试通过；覆盖率 line 26.07% / fn 50.28% / region 19.46%；详见《迁移测试对照表》《覆盖审计报告》）
> 文档状态：`B2-PART1-TESTED`（批次 1 实现与测试完成；V5 集成与 Java 侧执行未做）
> Java 模块：`/Users/wandl/workspaces/workspace-github/WxJava/weixin-java-common`
> Java 包根：`/Users/wandl/workspaces/workspace-github/WxJava/weixin-java-common/src/main/java/me/chanjar/weixin/common`
> Rust 目标：`crates/wx-rust-common`

<!-- current-migration-contract-start -->
## 当前迁移规范执行口径

- 本文是当前权威语义台账；模板生成仅为 `DRAFT`，不得以表格存在、编译或测试全绿推断语义迁移完成。
- Java 源码与调用链定义必须保持的 Advice/Interceptor、生命周期、副作用、错误、并发及扩展语义；Rust 依赖只定义可精确复用的实现边界。
- 路径按 `/Users/wandl/workspaces/workspace-github/WxJava/weixin-java-common/src/main/java/me/chanjar/weixin/common` 后末 `2` 层计算；目录或对象未对齐时，即使局部语义测试通过仍属于未完成。
- `DEPENDENCY_REUSED` 必须记录 crate、版本/提交、源码符号、本地适配器和集成测试；“相似能力”“生态已有”不构成复用证据。
- 只有 `IMPLEMENTED`、`DEPENDENCY_REUSED`、`PLATFORM_NA` 计入已处理；历史说明只能进入文末附录，不能覆盖当前事实。
<!-- current-migration-contract-end -->

本文回答“功能是否存在、Rust 如何实现、可观察行为是否一致”。每个完成结论必须链接 Java/Rust 文件或符号、测试/oracle、命令及适用 artifact。

> 执行规则：编码前一次性锁定全部语义合同；B2 连续完成整批实现与测试代码，
> 但不运行逐对象验证；实现冻结后统一填写验证基线并批量升级状态。

## 一、核心能力与调用链

| 能力 | Java 入口与调用链 | Rust 入口与调用链 | 语义差异 | 状态 | 证据 |
|---|---|---|---|---|---|
| AccessToken 获取（模式 D） | `BaseWxMpServiceImpl.getAccessToken(force)`：双检锁 + tryLock(100ms/3s 超时) + 模板方法 `doGetAccessTokenRequest` / `doGetStableAccessTokenRequest` | `async fn get_access_token(&self, force) -> Result<String>` + `tokio::sync::Mutex` 超时抢锁 + trait 模板方法 | Java 同步锁 → Rust async Mutex；超时语义一致（3s 抛超时） | `MISSING` | 并发差分测试：N 任务并发刷新仅 1 次 HTTP |
| 请求执行引擎（模式 E） | `execute/executeInternal`：错误码 -1 指数退避重试（sleepMillis=1000<<n，maxRetry=5）；token 过期（ACCESS_TOKEN_ERROR_CODES）自动单次刷新，`doNotAutoRefresh` 防无限递归 | `async fn execute`：loop + `tokio::time::sleep` 退避；token 失效 → `expire_access_token` → 单次重试 | 递归 → 循环 + flag；重试次数/间隔/刷新次数保持一致 | `MISSING` | 参数化重试差分测试（0~5 次、错误码 -1/40001/42001） |
| 消息重复检查 | `WxMessageDuplicateChecker` 接口 + 内存（含单例）/Redis 实现 | `trait WxMessageDuplicateChecker` + 内存（`DashMap`）/Redis（`redis` crate）实现 | 一致 | `IMPLEMENTED` | `duplicate_checker_ttl_behavior`/`expiry`/`singleton_same_instance`/`concurrent_safe`（V1 通过；Redis 后端 V5 待做） |
| 会话管理 | `WxSessionManager` 会话生命周期 | `trait WxSessionManager` + 内存实现 | Java HttpSession 语义 → Rust 会话存储 | `IMPLEMENTED` | `session_get_same_instance`/`session_attributes_roundtrip`/`session_invalidate`/`session_invalidate_and_active_count`（V1 通过） |
| 文件工具 | `FileUtils.createTmpFile`（指定/默认目录）+ `imageToBase64ByStream` | `create_tmp_file(content, name, ext, Option<Path>)` + `image_to_base64_by_stream(reader)` | Java 默认 `java.io.tmpdir` → Rust `temp_dir()/wxjava-temp`（ADAPTED）；Java 单次 `read()` 可能截断 → Rust `read_to_end` 完整读取（修复语义） | `IMPLEMENTED` | `source_parity_fs_utils`（6 个测试；黄金向量 `aGVsbG8=`；错误路径 `Result::Err` 对应 Java `null`；V1 通过） |
| OAuth2/OCR/图片处理 | 三个服务接口 | async traits + reqwest | 一致 | `MISSING` | golden 差分 |

## 二、类型、所有权与可空性

| Java 语义 | Rust 映射 | 生命周期/所有权 | `null`/默认语义 | 状态 | 测试 |
|---|---|---|---|---|---|
| `String`（可空字段如 `errorMsgEn`） | `Option<String>` | owned | `None` ↔ null | `MISSING` | serde 空值测试 |
| `Integer`/`int` | `Option<i32>` / `i32` | owned/Copy | 明确区分 | `MISSING` | 边界测试 |
| `ReentrantLock` | `Arc<tokio::sync::Mutex<()>>` | `Arc` 共享 | 无 null | `MISSING` | 并发测试 |
| 反射（TypeAdapter 注册） | serde 派生 + 显式 registry | owned | 无 null | `MISSING` | 编译期替代 |

## 三、方法、重载与泛型

| Java 签名族 | Rust API | 映射方式 | 重载处理 | 泛型/trait 约束 | 行为差异 | 状态 |
|---|---|---|---|---|---|---|
| `WxError.fromJson(String)` / `fromJson(String, WxType)` | `WxError::from_json(&str)` / `from_json_with_type(&str, WxType)` | `ADAPTED` | 重载 → 语义后缀 `_with_type` | — | 错误码翻译（中文）行为一致 | `MISSING` |
| `WxService.get(String,String)` / `post(String,String)` | `async fn get(&self, url, query) -> Result<String>` / `post(...)` | `DIRECT` | 无重载 | — | 一致 | `MISSING` |
| `getAccessToken()` / `getAccessToken(boolean)` | `get_access_token()` / `get_access_token_with_force(force)` | `ADAPTED` | 默认参数重载 → 无参版委托 | — | 一致 | `MISSING` |

### JavaBean 与动态属性适配

| Java 字段/getter/setter | Rust 惯用 API | 脚本/动态属性 | registry/member resolver | 映射方式 | 双层验证 | 状态 |
|---|---|---|---|---|---|---|
| `WxAccessToken.getAccessToken()` / `setAccessToken(v)` | `WxAccessToken::access_token()` / `set_access_token(v)` | 无脚本动态访问 | 无需 registry | `ADAPTED` | getter 去 `get_` 前缀 | `MISSING` |
| `WxError.getErrorCode()` 等 | `WxError::error_code()` | 无脚本动态访问 | 无需 registry | `ADAPTED` | getter 去 `get_` 前缀 | `MISSING` |

## 四、错误体系

| Java 异常/错误路径 | Rust 错误类型 | 根因与上下文 | 重试/回滚语义 | 状态 | 差分测试 |
|---|---|---|---|---|---|
| `WxErrorException`（checked） | `#[derive(Error)] enum WxErrorException` | 根因链 + `#[source]` | 无重试（重试在引擎层） | `MISSING` | 抛错场景差分 |
| `WxRuntimeException` | `thiserror` 变体 | 运行时错误 | 无重试 | `MISSING` | 超时/中断场景 |
| `WxError` 错误码翻译 | `WxError` + 分模块中文错误码表 | 错误码 → 中文 | — | `MISSING` | 错误码表差分 |

## 五、序列化与数据格式

| Java/Jackson 行为 | serde 实现 | 字段/别名/默认/多态 | 线格式兼容 | 状态 | 黄金夹具 |
|---|---|---|---|---|---|
| AccessToken 获取（模式 D） | `BaseWxMpServiceImpl.getAccessToken(force)`：双检锁 + tryLock(100ms/3s 超时) + 模板方法 `doGetAccessTokenRequest` / `doGetStableAccessTokenRequest` | `async fn get_access_token(&self, force) -> Result<String>` + `tokio::sync::Mutex` 超时抢锁 + trait 模板方法 | Java 同步锁 → Rust async Mutex；超时语义一致（3s 抛超时） | `MISSING` | 并发差分测试：N 任务并发刷新仅 1 次 HTTP |
| 请求执行引擎（模式 E） | `execute/executeInternal`：错误码 -1 指数退避重试（sleepMillis=1000<<n，maxRetry=5）；token 过期（ACCESS_TOKEN_ERROR_CODES）自动单次刷新，`doNotAutoRefresh` 防无限递归 | `async fn execute`：loop + `tokio::time::sleep` 退避；token 失效 → `expire_access_token` → 单次重试 | 递归 → 循环 + flag；重试次数/间隔/刷新次数保持一致 | `MISSING` | 参数化重试差分测试（0~5 次、错误码 -1/40001/42001） |
| 消息重复检查 | `WxMessageDuplicateChecker` 接口 + 内存（含单例）/Redis 实现 | `trait WxMessageDuplicateChecker` + 内存（`DashMap`）/Redis（`redis` crate）实现 | 一致 | `IMPLEMENTED` | `duplicate_checker_ttl_behavior`/`expiry`/`singleton_same_instance`/`concurrent_safe`（V1 通过；Redis 后端 V5 待做） |
| 会话管理 | `WxSessionManager` 会话生命周期 | `trait WxSessionManager` + 内存实现 | Java HttpSession 语义 → Rust 会话存储 | `IMPLEMENTED` | `session_get_same_instance`/`session_attributes_roundtrip`/`session_invalidate`/`session_invalidate_and_active_count`（V1 通过） |
| 文件工具 | `FileUtils.createTmpFile`（指定/默认目录）+ `imageToBase64ByStream` | `create_tmp_file(content, name, ext, Option<Path>)` + `image_to_base64_by_stream(reader)` | Java 默认 `java.io.tmpdir` → Rust `temp_dir()/wxjava-temp`（ADAPTED）；Java 单次 `read()` 可能截断 → Rust `read_to_end` 完整读取（修复语义） | `IMPLEMENTED` | `source_parity_fs_utils`（6 个测试；黄金向量 `aGVsbG8=`；错误路径 `Result::Err` 对应 Java `null`；V1 通过） |
| OAuth2/OCR/图片处理 | 三个服务接口 | async traits + reqwest | 一致 | `MISSING` | golden 差分 |

## 六、集合、顺序与数值

| Java 语义 | Rust 容器/数值类型 | 顺序/重复/精度 | 边界差异 | 状态 | 测试 |
|---|---|---|---|---|---|
| AccessToken 获取（模式 D） | `BaseWxMpServiceImpl.getAccessToken(force)`：双检锁 + tryLock(100ms/3s 超时) + 模板方法 `doGetAccessTokenRequest` / `doGetStableAccessTokenRequest` | `async fn get_access_token(&self, force) -> Result<String>` + `tokio::sync::Mutex` 超时抢锁 + trait 模板方法 | Java 同步锁 → Rust async Mutex；超时语义一致（3s 抛超时） | `MISSING` | 并发差分测试：N 任务并发刷新仅 1 次 HTTP |
| 请求执行引擎（模式 E） | `execute/executeInternal`：错误码 -1 指数退避重试（sleepMillis=1000<<n，maxRetry=5）；token 过期（ACCESS_TOKEN_ERROR_CODES）自动单次刷新，`doNotAutoRefresh` 防无限递归 | `async fn execute`：loop + `tokio::time::sleep` 退避；token 失效 → `expire_access_token` → 单次重试 | 递归 → 循环 + flag；重试次数/间隔/刷新次数保持一致 | `MISSING` | 参数化重试差分测试（0~5 次、错误码 -1/40001/42001） |
| 消息重复检查 | `WxMessageDuplicateChecker` 接口 + 内存（含单例）/Redis 实现 | `trait WxMessageDuplicateChecker` + 内存（`DashMap`）/Redis（`redis` crate）实现 | 一致 | `IMPLEMENTED` | `duplicate_checker_ttl_behavior`/`expiry`/`singleton_same_instance`/`concurrent_safe`（V1 通过；Redis 后端 V5 待做） |
| 会话管理 | `WxSessionManager` 会话生命周期 | `trait WxSessionManager` + 内存实现 | Java HttpSession 语义 → Rust 会话存储 | `IMPLEMENTED` | `session_get_same_instance`/`session_attributes_roundtrip`/`session_invalidate`/`session_invalidate_and_active_count`（V1 通过） |
| 文件工具 | `FileUtils.createTmpFile`（指定/默认目录）+ `imageToBase64ByStream` | `create_tmp_file(content, name, ext, Option<Path>)` + `image_to_base64_by_stream(reader)` | Java 默认 `java.io.tmpdir` → Rust `temp_dir()/wxjava-temp`（ADAPTED）；Java 单次 `read()` 可能截断 → Rust `read_to_end` 完整读取（修复语义） | `IMPLEMENTED` | `source_parity_fs_utils`（6 个测试；黄金向量 `aGVsbG8=`；错误路径 `Result::Err` 对应 Java `null`；V1 通过） |
| OAuth2/OCR/图片处理 | 三个服务接口 | async traits + reqwest | 一致 | `MISSING` | golden 差分 |

## 七、并发、异步与生命周期

| Java 机制 | Rust 机制 | 原子性/背压/取消 | 关闭与资源回收 | 状态 | 并发证据 |
|---|---|---|---|---|---|
| `ReentrantLock`（token 锁） | `tokio::sync::Mutex` | 超时抢锁语义 | Drop 释放（无 finally） | `MISSING` | 并发刷新仅一次测试 |
| `ExecutorService`（Router 异步执行） | `tokio::task::spawn` | 任务取消 | JoinHandle Drop | `MISSING` | 任务取消测试 |
| `Thread.sleep`（重试退避） | `tokio::time::sleep` | 可取消 | — | `MISSING` | 重试差分 |

## 八、SPI、反射与动态分派

| Java 动态边界 | Rust 替代 | 注册/选择时机 | 扩展性差异 | 状态 | 测试 |
|---|---|---|---|---|---|
| Gson TypeAdapter 动态注册 | serde 编译期绑定 + 显式 registry | 编译期 | 无运行时反射 | `MISSING` | 编译测试 |
| `WxMessageDuplicateChecker` 多实现选择 | trait object / feature 门控 | 编译期 feature | 无动态加载 | `MISSING` | feature 测试 |

## 九、注解、代码生成与宏

| Java 注解 | 消费时机 | Rust 方案 | macro/runtime/adapter 边界 | 状态 | 编译测试 |
|---|---|---|---|---|---|
| `@Required` 等 | 编译期 | serde `#[serde]` + 校验函数 | derive | `MISSING` | 编译测试 |
| Lombok `@Data`/`@Builder` | 编译期 | `#[derive(Serialize, Deserialize, Clone, Debug)]` + builder | derive | `MISSING` | 生成 API 对比 |

## 十、组件替换与第三方依赖

| Java 职责/组件 | 必须保持的合同 | 搜索词/来源/观察日期 | Rust 形态/候选 | 版本/features/归属 | 硬门禁 | 适配度/生态健康/置信度 | 采用状态 | 证据/下一门禁 |
|---|---|---|---|---|---|---|---|---|
| Apache HttpClient/OkHttp/Jodd | 连接池/代理/超时/TLS/重定向/流式 | `reqwest blocking+async`；crates.io 2026-07 观察 | direct | 0.13.x | license Apache-2.0/MSRV 1.85/rustls | 适配高/健康/高 | `CANDIDATE` → `SPIKE_VERIFIED` 待 POC | 代理+流式 POC |
| Gson | JSON 字段/null 省略/多态 | `serde_json` preserve_order | direct | 1.0.x | MIT/Apache-2.0 | 适配高 | `CANDIDATE` | round-trip POC |
| Jedis/Redisson | Redis 操作 | `redis` crate | direct（feature） | 0.29.x | MIT | 适配中 | `CANDIDATE` | 集成 POC |
| SLF4J | 日志 | `tracing` | direct | 0.1.x | MIT/Apache-2.0 | 适配高 | `CANDIDATE` | 字段对齐 |
| Commons-Lang3 | 字符串/随机工具 | std + `rand` | std/direct | — | — | 适配高 | `CANDIDATE` | — |

### 已复用依赖对象

| Java 对象/精确职责 | crate | 版本/提交 | 上游源码符号 | 本地适配器/调用点 | 集成测试/oracle/命令 | 对象状态 |
|---|---|---|---|---|---|---|
| `SHA1` | sha1 | 0.11.0 | `sha1::Sha1` | 封装 `util/crypto/sha1.rs` | 已知向量差分 | `DEPENDENCY_REUSED`（待 POC） |
| 随机串生成 | rand | 0.10.x | `rand::random` | `util/random_utils.rs` | 长度/字符集测试 | `DEPENDENCY_REUSED`（待 POC） |

## 十一、框架与宿主集成

| Java 宿主 | Rust 适配器 | 核心隔离方式 | 启停/观测/错误 | 状态 | E2E |
|---|---|---|---|---|---|
| standalone（common 无框架依赖） | `wx-rust-common` 纯库 | 无 | 无启停 | `MISSING` | 库集成测试 |
| Spring（其他模块 starter 引用 common） | 由 `wx-rust-vernal` 集成层消费 | vernal 桥 | 装配期 | `MISSING` | E2E |

## 十二、示例、脚本与文档

| Java 资产 | Rust 对应 | 配置/输入 | 预期输出 | 状态 | 回放命令 |
|---|---|---|---|---|---|
| AccessToken 获取（模式 D） | `BaseWxMpServiceImpl.getAccessToken(force)`：双检锁 + tryLock(100ms/3s 超时) + 模板方法 `doGetAccessTokenRequest` / `doGetStableAccessTokenRequest` | `async fn get_access_token(&self, force) -> Result<String>` + `tokio::sync::Mutex` 超时抢锁 + trait 模板方法 | Java 同步锁 → Rust async Mutex；超时语义一致（3s 抛超时） | `MISSING` | 并发差分测试：N 任务并发刷新仅 1 次 HTTP |
| 请求执行引擎（模式 E） | `execute/executeInternal`：错误码 -1 指数退避重试（sleepMillis=1000<<n，maxRetry=5）；token 过期（ACCESS_TOKEN_ERROR_CODES）自动单次刷新，`doNotAutoRefresh` 防无限递归 | `async fn execute`：loop + `tokio::time::sleep` 退避；token 失效 → `expire_access_token` → 单次重试 | 递归 → 循环 + flag；重试次数/间隔/刷新次数保持一致 | `MISSING` | 参数化重试差分测试（0~5 次、错误码 -1/40001/42001） |
| 消息重复检查 | `WxMessageDuplicateChecker` 接口 + 内存（含单例）/Redis 实现 | `trait WxMessageDuplicateChecker` + 内存（`DashMap`）/Redis（`redis` crate）实现 | 一致 | `IMPLEMENTED` | `duplicate_checker_ttl_behavior`/`expiry`/`singleton_same_instance`/`concurrent_safe`（V1 通过；Redis 后端 V5 待做） |
| 会话管理 | `WxSessionManager` 会话生命周期 | `trait WxSessionManager` + 内存实现 | Java HttpSession 语义 → Rust 会话存储 | `IMPLEMENTED` | `session_get_same_instance`/`session_attributes_roundtrip`/`session_invalidate`/`session_invalidate_and_active_count`（V1 通过） |
| 文件工具 | `FileUtils.createTmpFile`（指定/默认目录）+ `imageToBase64ByStream` | `create_tmp_file(content, name, ext, Option<Path>)` + `image_to_base64_by_stream(reader)` | Java 默认 `java.io.tmpdir` → Rust `temp_dir()/wxjava-temp`（ADAPTED）；Java 单次 `read()` 可能截断 → Rust `read_to_end` 完整读取（修复语义） | `IMPLEMENTED` | `source_parity_fs_utils`（6 个测试；黄金向量 `aGVsbG8=`；错误路径 `Result::Err` 对应 Java `null`；V1 通过） |
| OAuth2/OCR/图片处理 | 三个服务接口 | async traits + reqwest | 一致 | `MISSING` | golden 差分 |

## 十三、明确不迁移与 Rust 新增

| 项目 | 分类 | 原因 | 替代/影响 | 覆盖率处理 | 批准 |
|---|---|---|---|---|---|
| `util.http.apache/hc/jodd/okhttp` 后端 | `PLATFORM_NA` | Java HTTP 客户端适配层；Rust reqwest 统一 | reqwest 唯一后端 | 单列排除 | 规划批准（PLAN.md 5.2 节） |
| `util.json.WxGsonBuilder` 手写适配器 | `PLATFORM_NA` | Gson 专属 | serde 派生替代 | 单列排除 | 规划批准 |
| native-image 配置 | `PLATFORM_NA` | GraalVM 专属 | Rust 原生二进制 | 单列排除 | 规划批准 |
| `reqwest` feature 化 HTTP 后端选择 | `RUST_EXTENSION` | Rust 生态能力 | 不影响兼容门面 | `RUST_EXTENSION` 单列 | 规划批准 |

## 十四、验证基线

仅在完整实现批次冻结后统一执行并填写以下各级证据：

| 证据级别 | 命令/环境 | 通过数 | 失败/跳过 | 结论 |
|---|---|---:|---|---|
| `V0_STATIC` 静态结构/无 stub | `audit_migration_layout.py`（B2 冻结后） | — | — | 待执行（规划阶段） |
| `V1_RUST_LOCAL` Rust 本地测试 | `cargo test --workspace`（B2 冻结后） | — | — | 待执行 |
| `V2_MIRRORED` Java 镜像合同 | 镜像 Java 测试用例（B2 冻结后） | — | — | 待执行 |
| `V3_GOLDEN_DIFF` Java golden 差分 | golden 夹具（B2 冻结后） | — | — | 待执行 |
| `V4_LIVE_DIFF` Java/Rust live 差分 | 同输入双实现（B2 冻结后） | — | — | 待执行 |
| `V5_HOST` 真实宿主/依赖 | redis/微信沙箱（B2 冻结后） | — | — | 待执行 |
| `V6_NONFUNCTIONAL` 并发/变异/负载/fuzz/安全 | 并发+proptest（B2 冻结后） | — | — | 待执行 |
| `V7_ROLLBACK` 灰度/回滚 | 记录恢复时间（B2 冻结后） | — | — | 待执行 |

### 测试三本账

| Ledger | 口径 | 完成/总数 | 关键缺口 | 证据 |
|---|---|---:|---|---|
| `SOURCE_PARITY` | 每个 Java 测试及独立参数化/动态用例有处置 | 0/29（规划） | 待 B0 枚举 | 待执行 |
| `RUST_OBLIGATION` | 目标机制引入的所有权、异步、错误、序列化、feature、macro、unsafe、adapter、组件风险 | 0/N（规划） | async 锁/取消/退避 | 待执行 |
| `VALUE_ADD` | 分支/mutant/property/fuzz/事故/负载/安全驱动且能说明可捕获缺陷 | 0/N（规划） | token 并发/重试差分 | 待执行 |

> Rust 覆盖率超过 Java 只有在工具、范围、profile/features/target 基本可比时才可声明；覆盖率不是行为一致性的替代证据。

> 完成防火墙：只要对象台账仍含 `MISSING`、`MISPLACED`、`STUB`、`PARTIAL` 或 `UNVERIFIED`，任何测试、覆盖率或组件证据都不能把模块结论升级为完成。

<!-- historical-design-appendix-start -->
## 历史设计附录

<!-- 合并旧语义文档仍有效的设计依据。旧状态、旧测试数和旧完成结论只能作为历史背景。 -->
